# FastAPI 后端地址（Umi Max 前缀必须为 UMI_APP_）
UMI_APP_API_BASE_URL=http://127.0.0.1:8000

# code-inspector-plugin 打开源码使用的 IDE
CODE_EDITOR=code
# macOS 如遇 IDE 识别失败，可改为绝对路径，例如：
# CODE_EDITOR=/usr/local/bin/code
