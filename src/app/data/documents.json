[
  {
    "id": "7b123db3-6611-441a-a506-a35b9139ae3a",
    "filename": "Python基础语法.md",
    "file_type": "md",
    "source_path": "/Users/wangzilong/Projects/my_python_project/src/app/data/uploads/69f99c9f-d2af-4429-999d-bf901b74398c_Python基础语法.md",
    "content": "下面这份可以当作你的 **Python 语法总纲 + 学习路线图**。\n目标不是“罗列所有冷门语法”，而是覆盖你日常开发里 **95% 会接触到的 Python 语法与写法**，并说明：\n\n* 它是什么\n* 什么时候用\n* 怎么写\n* 和 JS 哪里像、哪里不一样\n\n我会按由浅入深展开。\n\n---\n\n# 1. 先建立整体认知：Python 语法体系长什么样\n\n你可以把 Python 语法分成 10 大块：\n\n1. 基础语法：变量、注释、缩进、输入输出\n2. 数据类型：数字、字符串、布尔、列表、元组、字典、集合\n3. 运算与表达式：算术、比较、逻辑、成员、身份、三元表达式\n4. 流程控制：if、for、while、break、continue、match\n5. 函数：参数、返回值、作用域、匿名函数、闭包、装饰器\n6. 异常处理：try / except / finally / raise\n7. 模块与包：import、from import、自定义模块、包结构\n8. 面向对象：class、继承、封装、多态、特殊方法\n9. 迭代器与生成器：iter、next、yield、生成式\n10. 高阶语法：上下文管理器、类型注解、异步、数据类、模式匹配\n\n你可以简单理解为：\n\n* **基础语法** = 会不会写\n* **函数 / 类 / 模块** = 能不能组织代码\n* **异常 / 上下文 / 异步** = 能不能写工程代码\n* **生成器 / 装饰器 / 闭包** = 能不能看懂高级代码\n\n---\n\n# 2. Python 最基础的语法规则\n\n## 2.1 缩进是语法的一部分\n\nPython 不像 JS 用 `{}` 表示代码块，它用 **缩进**。\n\n```python\nif True:\n    print(\"hello\")\n    print(\"world\")\n```\n\n### 使用场景\n\n任何代码块：if、for、while、函数、类、try 等。\n\n### JS 对比\n\nJS：\n\n```javascript\nif (true) {\n  console.log(\"hello\");\n  console.log(\"world\");\n}\n```\n\nPython：\n\n```python\nif True:\n    print(\"hello\")\n    print(\"world\")\n```\n\n### 关键区别\n\n* JS 靠 `{}` 分块\n* Python 靠缩进分块\n* Python 通常约定 4 个空格，不建议 tab 和空格混用\n\n---\n\n## 2.2 注释\n\n### 单行注释\n\n```python\n# 这是注释\nprint(\"hello\")\n```\n\n### 多行说明\n\nPython没有真正独立的“多行注释语法”，常见是：\n\n```python\n\"\"\"\n这是多行字符串，\n常被当作文档说明来写\n\"\"\"\n```\n\n更准确地说，这其实是字符串字面量，不是专门的注释符号。\n\n### JS 对比\n\nJS：\n\n```javascript\n// 单行注释\n/*\n多行注释\n*/\n```\n\nPython：\n\n```python\n# 单行注释\n\"\"\"\n文档字符串 / 多行说明\n\"\"\"\n```\n\n---\n\n## 2.3 变量定义\n\nPython 变量不需要 `let`、`const`、`var`。\n\n```python\nname = \"Zilong\"\nage = 18\nis_ok = True\n```\n\n### 使用场景\n\n存储任意值。\n\n### JS 对比\n\nJS：\n\n```javascript\nlet name = \"Zilong\";\nconst age = 18;\n```\n\nPython：\n\n```python\nname = \"Zilong\"\nage = 18\n```\n\n### 关键区别\n\nPython 变量是**名字绑定到对象**，不是传统意义的“固定类型盒子”。\n\n---\n\n## 2.4 输出\n\n```python\nprint(\"hello\")\nprint(\"name =\", name)\n```\n\n### 使用场景\n\n调试、命令行程序输出、快速验证逻辑。\n\n### JS 对比\n\nJS 常用：\n\n```javascript\nconsole.log(\"hello\");\n```\n\nPython 常用：\n\n```python\nprint(\"hello\")\n```\n\n---\n\n# 3. Python 的核心数据类型\n\n---\n\n## 3.1 数字类型\n\n### 整数 int\n\n```python\na = 10\n```\n\n### 浮点数 float\n\n```python\nb = 3.14\n```\n\n### 复数 complex\n\n```python\nc = 1 + 2j\n```\n\n复数平时业务开发很少用。\n\n### 使用场景\n\n* int：计数、索引、ID、循环\n* float：价格、长度、比率、计算\n* complex：科学计算\n\n### JS 对比\n\nJS 绝大多数数字都是 `number`，Python 区分 `int` 和 `float` 更明显。\n\n---\n\n## 3.2 布尔类型 bool\n\n```python\nis_login = True\nis_admin = False\n```\n\n### 注意\n\nPython 里首字母大写：\n\n* `True`\n* `False`\n* `None`\n\n不是 JS 的 `true false null`\n\n---\n\n## 3.3 字符串 str\n\n```python\nname = \"python\"\nmsg = 'hello'\ntext = \"\"\"\n这是多行字符串\n第二行\n\"\"\"\n```\n\n### 常见操作\n\n```python\ns = \"hello\"\nprint(len(s))       # 5\nprint(s[0])         # h\nprint(s[-1])        # o\nprint(s[1:4])       # ell\nprint(s.upper())    # HELLO\nprint(s.replace(\"h\", \"H\"))  # Hello\n```\n\n### 使用场景\n\n文本处理、日志、接口数据、文件内容。\n\n### JS 对比\n\nJS 字符串也支持索引和方法，但 Python 切片非常强：\n\n```python\ns[1:4]\n```\n\nJS 里更常见：\n\n```javascript\ns.slice(1, 4)\n```\n\n---\n\n## 3.4 列表 list\n\nPython 的列表，类似 JS 的数组。\n\n```python\nnums = [1, 2, 3]\nnames = [\"a\", \"b\", \"c\"]\nmixed = [1, \"hello\", True]\n```\n\n### 常见操作\n\n```python\nnums.append(4)\nnums.insert(0, 100)\nnums.remove(2)\nlast = nums.pop()\nprint(nums[0])\nprint(nums[1:3])\n```\n\n### 使用场景\n\n* 存一组有顺序的数据\n* 遍历结果集\n* 接口返回列表\n* 批量处理\n\n### JS 对比\n\nJS：\n\n```javascript\nconst arr = [1, 2, 3];\narr.push(4);\n```\n\nPython：\n\n```python\nnums = [1, 2, 3]\nnums.append(4)\n```\n\n### 区别\n\n* Python list 更像可变数组\n* 支持切片、推导式，写数据处理时很强\n\n---\n\n## 3.5 元组 tuple\n\n元组和列表很像，但**通常不可修改**。\n\n```python\npoint = (10, 20)\nperson = (\"Tom\", 18)\n```\n\n### 使用场景\n\n* 表示固定结构的数据\n* 函数返回多个值\n* 不希望被修改的轻量结构\n\n### 例子\n\n```python\ndef get_user():\n    return (\"Tom\", 18)\n\nname, age = get_user()\n```\n\n### JS 对比\n\nJS 没有真正一模一样的 tuple 语法概念。TS 有 tuple 类型概念，但运行时还是数组。\n\n---\n\n## 3.6 字典 dict\n\nPython 字典类似 JS 的对象 / Map 中的“键值对存储”。\n\n```python\nuser = {\n    \"name\": \"Tom\",\n    \"age\": 18,\n    \"is_admin\": True\n}\n```\n\n### 常见操作\n\n```python\nprint(user[\"name\"])\nprint(user.get(\"age\"))\nuser[\"city\"] = \"Shanghai\"\ndel user[\"age\"]\n```\n\n### 使用场景\n\n* JSON 风格数据\n* 配置项\n* 按 key 查值\n* 接口响应处理\n\n### JS 对比\n\nJS：\n\n```javascript\nconst user = { name: \"Tom\", age: 18 };\nconsole.log(user.name);\n```\n\nPython：\n\n```python\nuser = {\"name\": \"Tom\", \"age\": 18}\nprint(user[\"name\"])\n```\n\n### 区别\n\nPython 更推荐显式地按 key 取值；复杂场景常配合类型注解、dataclass、Pydantic 使用。\n\n---\n\n## 3.7 集合 set\n\n集合特点：**无序、去重**。\n\n```python\ns = {1, 2, 3}\n```\n\n### 常见操作\n\n```python\ns.add(4)\ns.remove(2)\nprint(1 in s)\n```\n\n### 使用场景\n\n* 去重\n* 成员判断\n* 交集、并集、差集\n\n### 例子\n\n```python\na = {1, 2, 3}\nb = {3, 4, 5}\nprint(a & b)   # 交集 {3}\nprint(a | b)   # 并集\nprint(a - b)   # 差集\n```\n\n### JS 对比\n\nJS 有 `Set`，概念比较接近。\n\n---\n\n## 3.8 None\n\n```python\nresult = None\n```\n\n表示“空值 / 没有值”。\n\n### JS 对比\n\n接近 `null`，但也有点像 `undefined` 的一部分语义。\n\nPython 里最常见的是判断：\n\n```python\nif result is None:\n    print(\"没有结果\")\n```\n\n---\n\n# 4. 运算符与表达式\n\n---\n\n## 4.1 算术运算\n\n```python\na = 10 + 2\nb = 10 - 2\nc = 10 * 2\nd = 10 / 2\ne = 10 // 3   # 整除\nf = 10 % 3    # 取余\ng = 2 ** 3    # 幂运算\n```\n\n### 使用场景\n\n计数、分页、数学计算、索引计算。\n\n### JS 对比\n\nJS 没有 `**` 之前常用 `Math.pow()`，现在也支持 `**`。\n\n---\n\n## 4.2 比较运算\n\n```python\nprint(1 == 1)\nprint(2 != 1)\nprint(3 > 2)\nprint(3 >= 3)\n```\n\n---\n\n## 4.3 逻辑运算\n\n```python\na = True and False\nb = True or False\nc = not True\n```\n\n### JS 对比\n\nJS 是 `&& || !`\nPython 是 `and or not`\n\n---\n\n## 4.4 成员运算\n\n```python\nprint(\"a\" in \"abc\")\nprint(2 in [1, 2, 3])\nprint(\"name\" in {\"name\": \"Tom\"})\n```\n\n### 使用场景\n\n检查元素是否存在。\n\n---\n\n## 4.5 身份运算 is\n\n```python\nx = None\nprint(x is None)\n```\n\n### 这个很重要\n\n`is` 比较的是 **是不是同一个对象**\n`==` 比较的是 **值是否相等**\n\n### 推荐记忆\n\n判断 `None` 时，优先用：\n\n```python\nif x is None:\n    ...\n```\n\n而不是：\n\n```python\nif x == None:\n    ...\n```\n\n### JS 对比\n\nJS 的 `===` 是严格相等，但本质也不是 Python 的 `is`。\n\n---\n\n## 4.6 三元表达式\n\n```python\nage = 18\nstatus = \"adult\" if age >= 18 else \"child\"\n```\n\n### JS 对比\n\nJS：\n\n```javascript\nconst status = age >= 18 ? \"adult\" : \"child\";\n```\n\nPython：\n\n```python\nstatus = \"adult\" if age >= 18 else \"child\"\n```\n\n---\n\n# 5. 流程控制\n\n---\n\n## 5.1 if / elif / else\n\n```python\nscore = 85\n\nif score >= 90:\n    print(\"A\")\nelif score >= 80:\n    print(\"B\")\nelse:\n    print(\"C\")\n```\n\n### 使用场景\n\n分支判断、校验、状态流转。\n\n### JS 对比\n\nJS 是 `else if`，Python 是 `elif`。\n\n---\n\n## 5.2 while 循环\n\n```python\ni = 0\nwhile i < 5:\n    print(i)\n    i += 1\n```\n\n### 使用场景\n\n不确定循环次数、持续读取、重试逻辑。\n\n### JS 对比\n\n概念一样。\n\n---\n\n## 5.3 for 循环\n\nPython 的 `for` 不是传统 C 风格的三段式循环，\n它本质是 **遍历可迭代对象**。\n\n```python\nfor x in [1, 2, 3]:\n    print(x)\n```\n\n### 使用场景\n\n遍历列表、字典、字符串、文件、生成器。\n\n### JS 对比\n\n更接近 JS 的：\n\n```javascript\nfor (const x of arr) {\n  console.log(x);\n}\n```\n\n不是传统的：\n\n```javascript\nfor (let i = 0; i < arr.length; i++) {}\n```\n\n---\n\n## 5.4 range()\n\n```python\nfor i in range(5):\n    print(i)\n```\n\n输出 0 到 4。\n\n### 常见形式\n\n```python\nrange(5)        # 0,1,2,3,4\nrange(1, 5)     # 1,2,3,4\nrange(1, 10, 2) # 1,3,5,7,9\n```\n\n### 使用场景\n\n按次数循环、带索引迭代。\n\n### JS 对比\n\nPython 没有原生 `for (let i = 0; i < 5; i++)` 这种形式，通常用 `range()`。\n\n---\n\n## 5.5 break / continue / pass\n\n### break\n\n终止循环\n\n```python\nfor i in range(10):\n    if i == 5:\n        break\n    print(i)\n```\n\n### continue\n\n跳过本次循环\n\n```python\nfor i in range(5):\n    if i == 2:\n        continue\n    print(i)\n```\n\n### pass\n\n占位，不做任何事\n\n```python\nif True:\n    pass\n```\n\n### 使用场景\n\n* `break`：找到目标就结束\n* `continue`：跳过不合法数据\n* `pass`：先把结构写出来，后面再补实现\n\n---\n\n## 5.6 for-else / while-else\n\n这个是 Python 比较有特色的语法。\n\n```python\nfor x in [1, 2, 3]:\n    if x == 5:\n        break\nelse:\n    print(\"没有找到\")\n```\n\n### 含义\n\n`else` 在**循环正常结束且没有 break** 时执行。\n\n### 使用场景\n\n查找场景挺适合。\n\n---\n\n## 5.7 match-case（模式匹配）\n\nPython 3.10+。\n\n```python\nstatus = 200\n\nmatch status:\n    case 200:\n        print(\"success\")\n    case 404:\n        print(\"not found\")\n    case _:\n        print(\"unknown\")\n```\n\n### 使用场景\n\n多分支匹配，比一长串 `if elif` 更清晰。\n\n### JS 对比\n\n有点像 `switch`，但比 `switch` 更强。\n\nJS：\n\n```javascript\nswitch (status) {\n  case 200:\n    console.log(\"success\");\n    break;\n  default:\n    console.log(\"unknown\");\n}\n```\n\nPython：\n\n```python\nmatch status:\n    case 200:\n        print(\"success\")\n    case _:\n        print(\"unknown\")\n```\n\n---\n\n# 6. 函数：Python 开发的核心\n\n---\n\n## 6.1 定义函数\n\n```python\ndef greet():\n    print(\"hello\")\n```\n\n### 调用\n\n```python\ngreet()\n```\n\n### 使用场景\n\n封装逻辑、复用代码、拆分职责。\n\n---\n\n## 6.2 参数与返回值\n\n```python\ndef add(a, b):\n    return a + b\n\nresult = add(1, 2)\n```\n\n### JS 对比\n\nJS：\n\n```javascript\nfunction add(a, b) {\n  return a + b;\n}\n```\n\nPython：\n\n```python\ndef add(a, b):\n    return a + b\n```\n\n---\n\n## 6.3 默认参数\n\n```python\ndef greet(name=\"World\"):\n    print(\"Hello,\", name)\n```\n\n### 使用场景\n\n给参数提供默认值，减少调用负担。\n\n### 注意坑\n\n不要把可变对象当默认参数：\n\n```python\ndef foo(items=[]):   # 不推荐\n    items.append(1)\n    return items\n```\n\n应该写：\n\n```python\ndef foo(items=None):\n    if items is None:\n        items = []\n    items.append(1)\n    return items\n```\n\n这个是 Python 很经典的面试 / 工程坑。\n\n---\n\n## 6.4 关键字参数\n\n```python\ndef create_user(name, age):\n    print(name, age)\n\ncreate_user(age=18, name=\"Tom\")\n```\n\n### 使用场景\n\n参数很多时，提高可读性。\n\n### JS 对比\n\n有点像 JS 里传对象参数，但 Python 是直接按参数名传。\n\n---\n\n## 6.5 可变参数\n\n### `*args`\n\n接收位置参数的可变数量。\n\n```python\ndef total(*args):\n    return sum(args)\n\nprint(total(1, 2, 3))\n```\n\n### `**kwargs`\n\n接收关键字参数的可变数量。\n\n```python\ndef print_info(**kwargs):\n    print(kwargs)\n\nprint_info(name=\"Tom\", age=18)\n```\n\n### 使用场景\n\n* 包装函数\n* 转发参数\n* 写通用工具\n* 写装饰器\n\n### JS 对比\n\n有点像剩余参数和对象展开。\n\nJS：\n\n```javascript\nfunction total(...args) {}\n```\n\nPython：\n\n```python\ndef total(*args):\n    ...\n```\n\n---\n\n## 6.6 返回多个值\n\n```python\ndef get_user():\n    return \"Tom\", 18\n```\n\n调用：\n\n```python\nname, age = get_user()\n```\n\n本质上是返回了一个元组。\n\n### 使用场景\n\n函数返回多个结果。\n\n### JS 对比\n\n有点像：\n\n```javascript\nreturn [name, age];\n```\n\n然后解构。\n\n---\n\n## 6.7 作用域：局部变量、全局变量\n\n```python\nx = 10\n\ndef foo():\n    y = 20\n    print(x)\n    print(y)\n```\n\n### 修改全局变量\n\n```python\ncount = 0\n\ndef inc():\n    global count\n    count += 1\n```\n\n### 使用场景\n\n一般能不用 `global` 就别用，工程里不推荐大量依赖全局状态。\n\n---\n\n## 6.8 匿名函数 lambda\n\n```python\nadd = lambda a, b: a + b\nprint(add(1, 2))\n```\n\n### 使用场景\n\n简单函数，常用于排序、map、filter。\n\n```python\nusers = [{\"age\": 20}, {\"age\": 18}]\nusers.sort(key=lambda x: x[\"age\"])\n```\n\n### JS 对比\n\n很像箭头函数，但 Python 的 lambda 只能写**单表达式**，不能写多行逻辑。\n\n---\n\n## 6.9 闭包\n\n```python\ndef outer(x):\n    def inner(y):\n        return x + y\n    return inner\n\nf = outer(10)\nprint(f(5))  # 15\n```\n\n### 含义\n\n内部函数记住了外部函数的变量。\n\n### 使用场景\n\n* 函数工厂\n* 装饰器\n* 封装部分状态\n\n### JS 对比\n\n和 JS 闭包概念很像。\n\n---\n\n## 6.10 装饰器\n\n这是 Python 很重要的高级语法。\n\n```python\ndef log_decorator(func):\n    def wrapper():\n        print(\"before\")\n        func()\n        print(\"after\")\n    return wrapper\n\n@log_decorator\ndef say_hi():\n    print(\"hi\")\n\nsay_hi()\n```\n\n### `@xxx` 是什么\n\n相当于：\n\n```python\nsay_hi = log_decorator(say_hi)\n```\n\n### 使用场景\n\n* 日志\n* 权限校验\n* 计时\n* 缓存\n* 路由注册\n* 框架能力扩展\n\n### 你作为前端转 Python，需要特别理解\n\n这类写法在 Flask、FastAPI、Django、测试框架里非常常见。\n\nJS 里也有 decorator 提案 / TS 装饰器概念，但 Python 装饰器在实际工程中的存在感更强。\n\n---\n\n# 7. 常见内置数据处理语法\n\n---\n\n## 7.1 列表推导式\n\n```python\nnums = [1, 2, 3, 4]\nsquares = [x * x for x in nums]\n```\n\n### 带条件\n\n```python\nevens = [x for x in nums if x % 2 == 0]\n```\n\n### 使用场景\n\n简洁地生成新列表。\n\n### JS 对比\n\n类似：\n\n```javascript\nconst squares = nums.map(x => x * x);\nconst evens = nums.filter(x => x % 2 === 0);\n```\n\nPython 一条推导式经常能同时表达 map + filter。\n\n---\n\n## 7.2 字典推导式\n\n```python\nnums = [1, 2, 3]\nd = {x: x * x for x in nums}\n```\n\n### 使用场景\n\n快速构造映射表。\n\n---\n\n## 7.3 集合推导式\n\n```python\ns = {x * 2 for x in [1, 2, 2, 3]}\n```\n\n会自动去重。\n\n---\n\n## 7.4 生成器表达式\n\n```python\ng = (x * x for x in range(5))\n```\n\n和列表推导式很像，但不是一次性把全部数据放进内存。\n\n### 使用场景\n\n大数据量、惰性计算、节省内存。\n\n---\n\n# 8. 异常处理\n\n---\n\n## 8.1 try / except\n\n```python\ntry:\n    x = 1 / 0\nexcept ZeroDivisionError:\n    print(\"除零错误\")\n```\n\n### 使用场景\n\n文件读取、网络请求、类型转换、数据库、外部依赖调用。\n\n---\n\n## 8.2 捕获多个异常\n\n```python\ntry:\n    ...\nexcept (ValueError, TypeError) as e:\n    print(e)\n```\n\n---\n\n## 8.3 else / finally\n\n```python\ntry:\n    print(\"try\")\nexcept Exception:\n    print(\"except\")\nelse:\n    print(\"没有异常时执行\")\nfinally:\n    print(\"一定执行\")\n```\n\n### 使用场景\n\n* `else`：成功路径逻辑\n* `finally`：资源释放、收尾动作\n\n---\n\n## 8.4 主动抛出异常 raise\n\n```python\ndef divide(a, b):\n    if b == 0:\n        raise ValueError(\"b 不能为 0\")\n    return a / b\n```\n\n### 使用场景\n\n参数校验、业务规则失败、封装底层错误。\n\n---\n\n## 8.5 自定义异常\n\n```python\nclass UserNotFoundError(Exception):\n    pass\n```\n\n### 使用场景\n\n让错误语义更清晰，便于业务层处理。\n\n### 这是你前面问过的重点\n\n这表示“定义了一个新的异常类型”。\n\n---\n\n## 8.6 异常链 raise ... from ...\n\n```python\ntry:\n    int(\"abc\")\nexcept ValueError as e:\n    raise RuntimeError(\"解析用户输入失败\") from e\n```\n\n### 含义\n\n新异常是基于旧异常产生的，保留因果链。\n\n### 使用场景\n\n底层错误转换为业务错误，但不丢原始原因。\n\n---\n\n# 9. 模块与包\n\n---\n\n## 9.1 import\n\n```python\nimport math\nprint(math.sqrt(16))\n```\n\n---\n\n## 9.2 from ... import ...\n\n```python\nfrom math import sqrt\nprint(sqrt(16))\n```\n\n---\n\n## 9.3 起别名\n\n```python\nimport numpy as np\nfrom math import sqrt as s\n```\n\n### 使用场景\n\n简化命名、避免冲突。\n\n---\n\n## 9.4 自定义模块\n\n假设 `utils.py`：\n\n```python\ndef add(a, b):\n    return a + b\n```\n\n另一个文件：\n\n```python\nimport utils\nprint(utils.add(1, 2))\n```\n\n---\n\n## 9.5 包\n\n目录里有 `__init__.py`，通常表示一个包。\n\n```python\nproject/\n  main.py\n  utils/\n    __init__.py\n    math_utils.py\n```\n\n导入：\n\n```python\nfrom utils.math_utils import add\n```\n\n### 使用场景\n\n组织大项目代码。\n\n### JS 对比\n\n有点像模块目录，但 Python 的包概念更体系化。\n\n---\n\n# 10. 面向对象编程\n\n---\n\n## 10.1 定义类\n\n```python\nclass User:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\n    def say_hi(self):\n        print(f\"Hi, I am {self.name}\")\n```\n\n创建对象：\n\n```python\nu = User(\"Tom\", 18)\nu.say_hi()\n```\n\n### 使用场景\n\n封装状态和行为。\n\n### JS 对比\n\nJS：\n\n```javascript\nclass User {\n  constructor(name, age) {\n    this.name = name;\n    this.age = age;\n  }\n}\n```\n\nPython：\n\n```python\nclass User:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n```\n\n---\n\n## 10.2 self\n\n`self` 表示“当前实例对象”。\n\n```python\nself.name = name\n```\n\n### 你可以理解成\n\nJS 里的 `this`，但 Python 需要你显式写出来。\n\n---\n\n## 10.3 类属性和实例属性\n\n```python\nclass User:\n    species = \"human\"   # 类属性\n\n    def __init__(self, name):\n        self.name = name  # 实例属性\n```\n\n### 使用场景\n\n* 类属性：所有实例共享\n* 实例属性：每个对象独有\n\n---\n\n## 10.4 继承\n\n```python\nclass Animal:\n    def speak(self):\n        print(\"...\")\n\nclass Dog(Animal):\n    def speak(self):\n        print(\"wang\")\n```\n\n### 使用场景\n\n复用父类逻辑，扩展子类行为。\n\n---\n\n## 10.5 super()\n\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\nclass Dog(Animal):\n    def __init__(self, name, breed):\n        super().__init__(name)\n        self.breed = breed\n```\n\n### 使用场景\n\n调用父类构造或方法。\n\n### JS 对比\n\n和 JS class 里的 `super()` 很像。\n\n---\n\n## 10.6 特殊方法（魔术方法）\n\n### `__str__`\n\n```python\nclass User:\n    def __init__(self, name):\n        self.name = name\n\n    def __str__(self):\n        return f\"User({self.name})\"\n```\n\n### `__len__`\n\n```python\nclass Box:\n    def __len__(self):\n        return 10\n```\n\n### `__getitem__`\n\n```python\nclass MyList:\n    def __getitem__(self, index):\n        return index * 10\n```\n\n### 使用场景\n\n让对象表现得像字符串、容器、可比较对象等。\n\n这是 Python 非常强大的地方：**你可以通过协议让自己的对象像内置类型一样工作**。\n\n---\n\n## 10.7 封装与“私有”\n\nPython 没有 Java 那种严格私有字段机制，但有约定：\n\n```python\nclass User:\n    def __init__(self):\n        self._internal = 1\n        self.__secret = 2\n```\n\n### 约定\n\n* `_name`：约定为“内部使用”\n* `__name`：名称改写，避免直接访问\n\nPython 更强调“约定优于强限制”。\n\n---\n\n## 10.8 类方法与静态方法\n\n### 类方法\n\n```python\nclass User:\n    count = 0\n\n    @classmethod\n    def get_count(cls):\n        return cls.count\n```\n\n### 静态方法\n\n```python\nclass MathUtil:\n    @staticmethod\n    def add(a, b):\n        return a + b\n```\n\n### 使用场景\n\n* `@classmethod`：和类本身相关\n* `@staticmethod`：逻辑上放在类里，但不依赖实例和类状态\n\n---\n\n# 11. 迭代器、生成器、yield\n\n---\n\n## 11.1 可迭代对象\n\n能被 `for` 遍历的，一般就是可迭代对象。\n\n```python\nfor x in [1, 2, 3]:\n    print(x)\n```\n\n列表、元组、字典、字符串、集合、文件对象等都可以。\n\n---\n\n## 11.2 迭代器\n\n```python\nnums = [1, 2, 3]\nit = iter(nums)\nprint(next(it))\nprint(next(it))\n```\n\n### 含义\n\n* `iter()` 生成迭代器\n* `next()` 每次取下一个值\n\n### 使用场景\n\n理解 Python 遍历本质、手动控制迭代流程。\n\n---\n\n## 11.3 生成器函数\n\n```python\ndef gen():\n    yield 1\n    yield 2\n    yield 3\n```\n\n调用：\n\n```python\ng = gen()\nprint(next(g))\nprint(next(g))\n```\n\n### yield 是什么\n\n函数执行到 `yield` 时，先返回一个值，并暂停状态；下次继续从暂停处往下执行。\n\n### 使用场景\n\n* 流式处理\n* 大数据逐步生成\n* 惰性计算\n* 协程基础理解\n\n### 这个语法非常重要\n\n很多高级框架、异步思想、流式输出都和它相关。\n\n### JS 对比\n\nJS 也有 generator：\n\n```javascript\nfunction* gen() {\n  yield 1;\n}\n```\n\n概念相似。\n\n---\n\n# 12. 文件操作\n\n---\n\n## 12.1 打开文件\n\n```python\nf = open(\"test.txt\", \"r\", encoding=\"utf-8\")\ncontent = f.read()\nf.close()\n```\n\n### 模式\n\n* `r` 读\n* `w` 写，覆盖\n* `a` 追加\n* `b` 二进制\n* `x` 新建\n\n---\n\n## 12.2 推荐写法：with\n\n```python\nwith open(\"test.txt\", \"r\", encoding=\"utf-8\") as f:\n    content = f.read()\n```\n\n### 使用场景\n\n自动关闭文件，避免资源泄漏。\n\n### 这是工程代码标准写法\n\n以后你读文件、写文件，优先都用 `with`。\n\n---\n\n# 13. 上下文管理器\n\n`with` 背后是上下文管理协议。\n\n```python\nwith open(\"a.txt\", \"r\") as f:\n    data = f.read()\n```\n\n### 含义\n\n进入代码块前做准备，离开时自动清理。\n\n### 使用场景\n\n* 文件\n* 数据库连接\n* 锁\n* 临时资源\n* 网络连接\n\n### 高阶理解\n\n如果一个对象实现了 `__enter__` 和 `__exit__`，就可以配合 `with` 使用。\n\n---\n\n# 14. 常见内置函数\n\n这些非常常用，属于你必须熟的“基础工具箱”。\n\n---\n\n## 14.1 len()\n\n```python\nlen([1, 2, 3])\nlen(\"hello\")\n```\n\n---\n\n## 14.2 type()\n\n```python\nprint(type(123))\n```\n\n---\n\n## 14.3 isinstance()\n\n```python\nprint(isinstance(123, int))\n```\n\n### 使用场景\n\n判断对象类型，通常比 `type(x) == int` 更灵活。\n\n---\n\n## 14.4 enumerate()\n\n```python\nnames = [\"a\", \"b\", \"c\"]\nfor i, name in enumerate(names):\n    print(i, name)\n```\n\n### 使用场景\n\n遍历时同时拿索引和元素。\n\n### JS 对比\n\n类似：\n\n```javascript\narr.entries()\n```\n\n或 `forEach((item, index) => ...)`\n\n---\n\n## 14.5 zip()\n\n```python\nnames = [\"Tom\", \"Jerry\"]\nages = [18, 20]\n\nfor name, age in zip(names, ages):\n    print(name, age)\n```\n\n### 使用场景\n\n并行遍历多个序列。\n\n---\n\n## 14.6 sorted()\n\n```python\nnums = [3, 1, 2]\nprint(sorted(nums))\n```\n\n### 带 key\n\n```python\nusers = [{\"age\": 20}, {\"age\": 18}]\nprint(sorted(users, key=lambda x: x[\"age\"]))\n```\n\n### 使用场景\n\n排序数据。\n\n---\n\n## 14.7 map / filter / sum / min / max / any / all\n\n```python\nnums = [1, 2, 3]\n\nprint(sum(nums))\nprint(min(nums))\nprint(max(nums))\nprint(any([False, True, False]))\nprint(all([True, True, False]))\n```\n\n### 使用场景\n\n聚合、判断、函数式处理。\n\n不过在 Python 里，很多时候 **列表推导式比 map/filter 更直观**。\n\n---\n\n# 15. 解包语法\n\n---\n\n## 15.1 序列解包\n\n```python\na, b = 1, 2\nname, age = (\"Tom\", 18)\n```\n\n---\n\n## 15.2 星号解包\n\n```python\nfirst, *middle, last = [1, 2, 3, 4, 5]\n```\n\n结果：\n\n* `first = 1`\n* `middle = [2, 3, 4]`\n* `last = 5`\n\n### 使用场景\n\n处理不定长数据、拆分列表。\n\n---\n\n## 15.3 参数展开\n\n```python\ndef add(a, b, c):\n    return a + b + c\n\nnums = [1, 2, 3]\nprint(add(*nums))\n```\n\n字典展开：\n\n```python\ndef greet(name, age):\n    print(name, age)\n\ndata = {\"name\": \"Tom\", \"age\": 18}\ngreet(**data)\n```\n\n### JS 对比\n\n类似 spread：\n\n```javascript\nfn(...arr)\nconst obj2 = { ...obj1 }\n```\n\n---\n\n# 16. 类型注解\n\nPython 是动态类型，但支持类型提示。\n\n```python\ndef add(a: int, b: int) -> int:\n    return a + b\n```\n\n### 使用场景\n\n* 提升可读性\n* IDE 智能提示\n* 静态检查\n* 大型工程协作\n\n### 常见写法\n\n```python\nname: str = \"Tom\"\nage: int = 18\nitems: list[int] = [1, 2, 3]\nuser: dict[str, str] = {\"name\": \"Tom\"}\n```\n\n### JS / TS 对比\n\n如果你有 TS 基础，这个很好理解。\n但注意：**Python 类型注解默认不会在运行时强制拦截错误**，它主要是提示和检查用途。\n\n---\n\n# 17. dataclass\n\n```python\nfrom dataclasses import dataclass\n\n@dataclass\nclass User:\n    name: str\n    age: int\n```\n\n### 它解决什么问题\n\n如果一个类主要是存数据，自己手写 `__init__` 很烦，`@dataclass` 可以自动帮你生成。\n\n### 使用场景\n\n* DTO\n* 配置对象\n* 简单数据实体\n* 内部结构体\n\n### 对比 JS\n\n有点像“定义一个纯数据对象结构”，但 Python dataclass 更正式、更工程化。\n\n---\n\n# 18. 异步语法 async / await\n\n---\n\n## 18.1 定义异步函数\n\n```python\nasync def main():\n    print(\"hello\")\n```\n\n---\n\n## 18.2 await\n\n```python\nimport asyncio\n\nasync def say_hi():\n    await asyncio.sleep(1)\n    print(\"hi\")\n\nasyncio.run(say_hi())\n```\n\n### 使用场景\n\n* 网络请求\n* 并发 IO\n* Web 服务\n* 爬虫\n* 消息系统\n\n### JS 对比\n\n和 JS 的 `async / await` 很像。\n\nJS：\n\n```javascript\nasync function main() {\n  await fetchData();\n}\n```\n\nPython：\n\n```python\nasync def main():\n    await fetch_data()\n```\n\n### 但底层生态不同\n\nPython 异步通常围绕 `asyncio`，JS 围绕事件循环和 Promise。\n\n---\n\n## 18.3 async for / async with\n\n更高阶一点：\n\n```python\nasync for item in async_iterable:\n    ...\n```\n\n```python\nasync with resource:\n    ...\n```\n\n### 使用场景\n\n异步流、异步资源管理。\n\n---\n\n# 19. 高阶但常见的工程语法\n\n---\n\n## 19.1 property\n\n```python\nclass User:\n    def __init__(self, age):\n        self._age = age\n\n    @property\n    def age(self):\n        return self._age\n```\n\n### 使用场景\n\n把方法伪装成属性访问，便于封装校验逻辑。\n\n---\n\n## 19.2 断言 assert\n\n```python\nassert 1 + 1 == 2\n```\n\n### 使用场景\n\n调试、单元测试、前置条件验证。\n\n### 注意\n\n生产逻辑里不能把它当严格业务校验机制。\n\n---\n\n## 19.3 文档字符串 docstring\n\n```python\ndef add(a, b):\n    \"\"\"返回两个数字之和\"\"\"\n    return a + b\n```\n\n### 使用场景\n\n函数说明、自动文档、IDE 提示。\n\n---\n\n## 19.4 **name** == \"**main**\"\n\n```python\ndef main():\n    print(\"run\")\n\nif __name__ == \"__main__\":\n    main()\n```\n\n### 含义\n\n当前文件被直接运行时执行，不是被 import 时执行。\n\n### 使用场景\n\n脚本入口。\n\n### JS 对比\n\n有点像 Node 里的：\n\n```javascript\nif (require.main === module) {}\n```\n\n---\n\n# 20. 你真正开发时最常用的语法子集\n\n如果从“实战频率”看，下面这些是最高频：\n\n## 入门必会\n\n* 变量\n* 字符串\n* list / dict\n* if / for / while\n* 函数\n* import\n* try / except\n* 文件读写\n* 类的基础用法\n\n## 工程必会\n\n* 默认参数\n* `*args` / `**kwargs`\n* 列表推导式\n* lambda\n* with\n* 自定义异常\n* 装饰器基础\n* 类型注解\n* dataclass\n\n## 看源码必会\n\n* 闭包\n* 装饰器\n* yield / generator\n* 魔术方法\n* classmethod / staticmethod\n* async / await\n\n---\n\n# 21. Python 和 JS 的核心差异，建议你重点建立这些认知\n\n---\n\n## 21.1 Python 更强调“可读性优先”\n\nJS 很多时候偏灵活、生态驱动。\nPython 很多语法设计目标就是：**让人读着像自然语言**。\n\n例如：\n\n```python\nif x in items:\n    ...\n```\n\n```python\nfor name, age in zip(names, ages):\n    ...\n```\n\n---\n\n## 21.2 Python 的 for 本质是遍历，不是传统计数循环\n\n你要从 JS 的“三段式 for”思维切换到“遍历 iterable”。\n\n---\n\n## 21.3 Python 对数据处理特别强\n\n* 切片\n* 推导式\n* zip\n* enumerate\n* sorted(key=...)\n* dict / set 操作\n\n这套东西做后端、脚本、AI 数据处理时非常顺手。\n\n---\n\n## 21.4 Python 的高级语法在框架里存在感很强\n\n例如：\n\n* `@decorator`\n* `yield`\n* `with`\n* `async def`\n* `@property`\n* dataclass\n* 类型注解\n\n你如果学 FastAPI、Django、机器学习框架，会频繁看到。\n\n---\n\n## 21.5 Python 更像“协议驱动”\n\n很多能力不是靠“硬编码继承某个类”，而是对象实现某种协议：\n\n* 可迭代协议\n* 上下文管理协议\n* 魔术方法协议\n* 异步协议\n\n这点和 JS 的对象模型思路不太一样。\n\n---\n\n# 22. 给你一份推荐学习顺序\n\n按你现在的情况，我建议按这个顺序学：\n\n## 第一阶段：先把基本语法打通\n\n1. 变量、缩进、注释\n2. 数字、字符串、布尔\n3. list / tuple / dict / set\n4. 运算符\n5. if / for / while\n6. 函数\n7. import\n8. try / except\n\n## 第二阶段：进入工程开发\n\n9. 文件读写\n10. with\n11. 类与对象\n12. 继承\n13. 列表推导式\n14. lambda\n15. 类型注解\n16. dataclass\n\n## 第三阶段：开始能看中高级代码\n\n17. 闭包\n18. 装饰器\n19. yield / generator\n20. 魔术方法\n21. async / await\n22. property / classmethod / staticmethod\n\n---\n\n# 23. 一个完整的小例子，把前面语法串起来\n\n```python\nfrom dataclasses import dataclass\nfrom typing import Optional\n\n\nclass UserNotFoundError(Exception):\n    pass\n\n\n@dataclass\nclass User:\n    name: str\n    age: int\n\n\ndef find_user(users: list[User], name: str) -> Optional[User]:\n    for user in users:\n        if user.name == name:\n            return user\n    return None\n\n\ndef main():\n    users = [\n        User(\"Tom\", 18),\n        User(\"Jerry\", 20),\n    ]\n\n    try:\n        target = find_user(users, \"Tom\")\n        if target is None:\n            raise UserNotFoundError(\"用户不存在\")\n\n        status = \"adult\" if target.age >= 18 else \"child\"\n        print(f\"name={target.name}, age={target.age}, status={status}\")\n\n    except UserNotFoundError as e:\n        print(f\"业务错误: {e}\")\n    except Exception as e:\n        print(f\"未知错误: {e}\")\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\n这个例子里已经包含了：\n\n* import\n* dataclass\n* 自定义异常\n* 类型注解\n* list\n* for\n* if\n* return\n* None\n* raise\n* try / except\n* 三元表达式\n* f-string\n* main 入口\n\n这就是很典型的 Python 工程基础代码风格。\n\n---\n\n# 24. 最后给你一个结论\n\n如果你想把 Python 语法学到“能开发、能看框架、能做 AI 工程”的程度，核心不是死记所有冷门语法，而是先把这几层吃透：\n\n## 第一层：会写\n\n* 变量\n* 数据类型\n* 条件\n* 循环\n* 函数\n\n## 第二层：会组织代码\n\n* 模块\n* 类\n* 异常\n* 文件操作\n* with\n\n## 第三层：会看工程代码\n\n* 装饰器\n* 闭包\n* 生成器\n* 类型注解\n* 异步\n\n你把这三层打通，Python 语法基本就真的覆盖到 95% 了。\n\n如果你要，我下一步可以继续直接给你整理一份：\n\n**《Python 语法覆盖 95% 的系统学习清单（表格版）》**\n\n我会按下面字段输出，适合你保存和反复查：\n\n* 语法点\n* 作用\n* 使用场景\n* 示例\n* 常见坑\n* JS 对比\n* 学习优先级",
    "status": "completed",
    "chunk_count": 265,
    "char_count": 22735,
    "error_message": null,
    "created_at": "2026-05-14T12:26:33.497730",
    "updated_at": "2026-05-14T12:26:33.502609"
  },
  {
    "id": "805a4712-e132-460e-9e36-b22b6a54504c",
    "filename": "text.txt",
    "file_type": "txt",
    "source_path": "/Users/wangzilong/Projects/my_python_project/src/app/data/uploads/2c2d8b9c-d2e0-4d24-bddb-0664e0205fc8_text.txt",
    "content": "第五周教程：文档上传 Document Upload + Chunk 切分\n本教程基于我实际读取到的 GitHub 仓库 wangzi6224/my_python_project 当前代码状态编写，不是凭空设计。\n\n我核对到的关键现状：\n\nroutes.py 当前仍是集中式路由文件，已经有 /chat、/chat/stream、会话、消息、context-preview、summary 等接口；其中 ContextBuilder 被 import 但没有被使用，建议删除。\ncontext_builder.py 当前已经实现 system prompt、summary、recent messages、max_context_chars、estimated_tokens、context preview；但 import 了未使用的 ContextPreviewResponse，建议删除。\nconversation_store.py 当前已经用 JSON 存储 conversations.json 和 messages.json，并且 create_conversation 已初始化 summarized_message_count 和 summary_updated_at。\nconversation_service.py 当前普通消息和流式消息都会先保存 user message，再构造上下文，再调用模型，再保存 assistant message；assistant 保存后才尝试自动 summary。\nstream_conversation_message 里 message_start 在模型流式输出前发送，自动 summary 在 assistant message 保存后才触发，因此 message_start 不会被 summary 阻塞。\nschemas.py 当前仍是集中式 Schema 文件，已有 Chat、Conversation、Message、ContextPreview、Summary 相关模型。第五周继续放这里，暂不拆分。\npaths.py 当前 DATA_DIR = BASE_ABS_DIR / \"data\"，也就是说实际数据目录是 src/app/data，不是仓库根目录的 data/。第五周建议沿用当前 DATA_DIR，新增 UPLOAD_DIR = DATA_DIR / \"uploads\"。\npyproject.toml 当前依赖里没有 python-multipart，上传文件接口必须补这个依赖。\n你的总体学习计划里 Week 5 的主线就是“上传文档 -> 解析文本 -> 切分 chunk -> 保存 document 和 chunk”，并明确第一版只做 .md / .txt，暂不做 PDF。\n1. 当前项目状态审视\n为什么要先审视\n第五周不是重新做一个上传 Demo，而是在你已经完成的 AI Chat Backend 上继续演进。\n\n当前项目已经具备：\n\nFastAPI\nOllamaProvider\nStreaming\nConfig\nLogger\nException Handler\nConversation\nMessage\nContextBuilder\nSummary Memory\nJSON Store\n所以第五周不要推翻现有结构，而是新增一条独立能力链路：\n\nDocument Upload\n  ↓\nDocument Loader\n  ↓\nChunk Splitter\n  ↓\nDocument Store\n  ↓\nDocuments API\n和现有会话链路并列：\n\nConversation / Message / ContextBuilder / Summarizer\n第五周最重要的工程原则是：\n\n文档处理逻辑不要污染会话系统。\n也就是说：\n\n不要把 document 存储塞进 conversation_store.py\n不要把 splitter 写在 routes.py\n不要让 loader 调用 LLM\n不要让 splitter 访问 JSON 文件\n当前真实代码对 Week 5 的影响\n你的项目当前有一个明显的低重构特点：\n\nschemas.py 集中管理 Pydantic Schema\nroutes.py 集中管理 API 路由\nconversation_store.py 使用 JSON 文件存储\nservices/ 下按业务拆服务\npaths.py 统一管理路径\n所以 Week 5 也延续这个风格：\n\n新增 document_store.py\n新增 services/document_loader.py\n新增 services/chunk_splitter.py\n新增 services/document_service.py\n\n修改 paths.py\n修改 schemas.py\n修改 routes.py\n修改 config.py，可选\n修改 README.md\n新增或更新 docs/dev-log.md\n暂不做：\n\nsrc/app/schemas/document.py\nsrc/app/api/documents.py\nSQLAlchemy\nPostgreSQL\npgvector\nEmbedding\nRAG\nAgent\nMCP\nPDF\n2. Week 4 收尾清理\n这部分只作为开始 Week 5 前的小清理，不作为第五周主线。\n\n2.1 删除 context_builder.py 未使用 import\n当前 context_builder.py 有：\n\nfrom src.app.schemas import ContextPreviewResponse\n但文件里没有使用它。建议删除。\n\n为什么\nContextBuilder 是服务层，不应该依赖 API Response Schema。\n\n它只需要返回普通 dict[str, Any]，由 routes 层包装成 ContextPreviewResponse。\n\n这和你前端经验类似：\n\nservice 返回普通数据\n页面或 controller 决定如何渲染 / 包装\n2.2 删除 routes.py 未使用 import\n当前 routes.py 有：\n\nfrom src.app.services.context_builder import ContextBuilder\n但 routes 里没有直接使用它，真正使用的是 get_context_preview service。建议删除。\n\n2.3 自动 summary 触发点确认\n当前普通消息流程：\n\n保存 user message\n构造 llm_messages\n调用 Ollama\n保存 assistant message\n_try_update_summary\nupdate_conversation\n这说明自动 summary 是在 assistant message 保存后触发的。\n\n这是正确的。\n\n如果 summary 在 assistant 保存前触发，会导致本轮回答没有被纳入摘要统计。\n\n2.4 流式 message_start 不会被 summary 阻塞\n当前流式流程：\n\n保存 user message\n构造 llm_messages\nyield message_start\n模型流式输出 delta\n保存 assistant message\n_try_update_summary\nyield message_end\nyield done\n所以 message_start 在 summary 之前很早就发出。\n\n这也是正确的。\n\n2.5 ContextBuilder 不会裁掉当前用户最新问题\n当前 build_messages 是从最近消息倒序选取，如果超出 max_context_chars，并且还没有选中任何最近消息，会强制保留这条消息。\n\n这说明即使当前用户问题很长，也不会出现“模型看不到用户最新问题”的严重 bug。\n\n2.6 create_conversation 已初始化 summary 字段\n当前 create_conversation 已有：\n\n\"summary\": None,\n\"summarized_message_count\": 0,\n\"summary_updated_at\": None,\n无需补。\n\n3. 第五周目标\n第五周目标定义为：\n\n实现 AI 知识库的最小文档处理层：\n支持上传 .md / .txt 文档，\n保存 Document 元数据和原始文本内容，\n解析文本，\n按标题 / 段落 / 长度策略切分为 Chunk，\n并提供文档列表、文档详情、Chunk 列表和 Chunk 预览接口。\n第五周结束后，项目从：\n\n只有 Chat / Conversation / Context Window 的 AI Chat Backend\n升级为：\n\n支持知识库文档上传、文本解析、Chunk 切分、Chunk 查询和调试预览的 AI Backend\n核心链路：\n\n用户上传文档\n  ↓\nFastAPI UploadFile 接收文件\n  ↓\n校验文件类型和大小\n  ↓\n保存原始文件到 DATA_DIR/uploads/\n  ↓\n解析文本内容\n  ↓\n创建 document 记录\n  ↓\n调用 ChunkSplitter 切分文本\n  ↓\n保存 document_chunks 记录\n  ↓\n返回 document_id、chunk_count、status\n  ↓\n用户可查询 documents 和 chunks\n4. 为什么 AI 知识库必须先有 Document / Chunk 层\n4.1 Document 是知识库的一级资源\nDocument 不是“上传文件记录”这么简单。\n\n它代表知识库里的一份原始资料。\n\n真实 AI 工程中，Document 解决这些问题：\n\n这份知识来自哪里？\n原始文件叫什么？\n文件保存在哪里？\n解析是否成功？\n什么时候上传？\n后续是否需要重建索引？\n失败原因是什么？\n对应字段：\n\nid\nfilename\nfile_type\nsource_path\ncontent\nstatus\nerror_message\nchar_count\nchunk_count\ncreated_at\nupdated_at\n类比前端 / Node.js：\n\nDocument 类似 CMS 里的 Article / Asset / Resource\n不是临时文件，而是业务资源。\n4.2 Chunk 是 RAG 检索的最小语义单元\nChunk 不是普通字符串切片。\n\n它是后续 Embedding 和 RAG 检索的最小单位。\n\n后面 Week 6 会做：\n\nchunk.content -> embedding vector -> pgvector\nWeek 7 会做：\n\n用户问题 -> query embedding -> 检索相关 chunks -> 构造 RAG prompt\n如果 Week 5 的 chunk 切得很差，后面 RAG 会直接变差。\n\n例如：\n\nchunk 太短：信息不足，检索到了也回答不了\nchunk 太长：主题混杂，embedding 表达模糊\nchunk 不保留 heading：回答时很难解释来源\nchunk 无 metadata：后续无法过滤、溯源、调试\n4.3 Loader 是 AI 数据入口\nUploadFile 不只是上传文件。\n\n在 AI 应用里，它是：\n\n非结构化数据进入系统的入口。\nLoader 负责把外部文件变成内部可处理的文本。\n\n本周只做：\n\n.md / .txt -> utf-8 text\n不做：\n\nPDF\nOCR\nWord\nExcel\nPPT\n4.4 Splitter 是影响召回质量的核心模块\nSplitter 不是工具函数。\n\n它决定：\n\n后续 embedding 表达什么语义\n后续 vector search 能不能召回正确内容\n后续 RAG 回答有没有可靠上下文\n第五周第一版策略：\n\nMarkdown：\n  优先按标题识别结构\n  每个标题下内容作为 section\n  section 太长时按段落拆\n  段落仍然太长时按字符长度兜底拆\n  chunk 保留 heading\n\ntxt：\n  按段落拆\n  段落太长时按字符长度兜底拆\n  heading = None\n4.5 为什么第五周不做 PDF\nPDF 解析复杂度明显高于 .md / .txt。\n\n它会引入：\n\n版面分析\n字体编码\n坐标\n多栏阅读顺序\n页眉页脚\n表格\n图片\nOCR\n扫描件\n文本提取误差\n这会严重偏离第五周主线。\n\n第五周目标不是“做一个全格式文档解析器”，而是建立 RAG 前置管线：\n\ndocument -> text -> chunk\nPDF 可以后续单独做专题。\n\n5. Document、Chunk、Loader、Splitter、Service 核心概念\n模块\t是什么\t真实工程价值\tNode / 前端类比\nDocument\t文档一级资源\t管理来源、状态、原文、统计信息\tCMS 的 Article / Asset\nDocumentChunk\t文档切分后的语义片段\tEmbedding / RAG 检索最小单元\t搜索索引里的 document segment\nDocumentLoader\t文件校验、保存、文本读取\t控制外部数据入口安全\tExpress multer upload middleware + parser\nChunkSplitter\t文本切分策略模块\t决定后续召回质量\t文本预处理 pipeline\nDocumentService\t编排层\t串起上传、解析、切分、保存\tNestJS Service / Application Service\ndocument_store.py\tJSON 存储层\t学习阶段可视化存储\tJSON 文件版 repository\n6. 第五周新增文件总览\n建议新增：\n\nsrc/app/document_store.py\n\nsrc/app/services/document_loader.py\nsrc/app/services/chunk_splitter.py\nsrc/app/services/document_service.py\n建议修改：\n\nsrc/app/paths.py\nsrc/app/schemas.py\nsrc/app/api/routes.py\nsrc/app/config.py\nREADME.md\ndocs/dev-log.md\npyproject.toml\n注意：\n\n当前 DATA_DIR 实际是：\n\nsrc/app/data\n所以第五周路径建议是：\n\nsrc/app/data/uploads/\nsrc/app/data/documents.json\nsrc/app/data/document_chunks.json\n如果你强制想用仓库根目录 data/，需要先统一修改 paths.py，但这会影响已有 conversations/messages 存储路径。第五周建议不动这个大方向。\n\nDay 1：设计 Document / Chunk Schema 和 JSON Store\n目标\n今天完成：\n\n1. 设计 Document 和 DocumentChunk 数据结构\n2. 修改 schemas.py\n3. 新增 document_store.py\n4. 新增 documents.json / document_chunks.json 的读写能力\n核心思想\n第五周先建立“数据模型”。\n\n没有数据模型，后面上传、解析、切分都会变成散乱逻辑。\n\n本日要形成两个资源：\n\nDocument：文档主记录\nDocumentChunk：文档切分记录\n为什么\n当前项目已经有 conversation_store.py 管理：\n\nconversations.json\nmessages.json\n第五周继续使用 JSON store，而不是直接上数据库。\n\n原因：\n\n学习阶段可直接打开 JSON 文件看结果\n和当前项目风格一致\n避免数据库迁移干扰主线\nWeek 6 再进入 PostgreSQL / pgvector 更自然\n但不要把 document 存进 conversation_store.py。\n\n因为：\n\nConversation 是聊天域\nDocument 是知识库域\nMessage 是对话消息\nChunk 是知识片段\n混在一起会导致 store 文件职责不清。\n\n怎么做\n第一步：修改 paths.py\n在当前基础上新增：\n\nUPLOAD_DIR = DATA_DIR / \"uploads\"\nDOCUMENTS_FILE = DATA_DIR / \"documents.json\"\nDOCUMENT_CHUNKS_FILE = DATA_DIR / \"document_chunks.json\"\n当前 paths.py 已经有 DATA_DIR。\n\n第二步：修改 schemas.py\n新增：\n\nDocumentItem\nDocumentDetailResponse\nDocumentListResponse\nUploadDocumentResponse\nDocumentChunkItem\nDocumentChunkListResponse\nDeleteDocumentResponse\n第三步：新增 document_store.py\n负责：\n\nload_documents\nsave_documents\ncreate_document\nupdate_document\nget_document\nlist_documents\ndelete_document\n\nload_document_chunks\nsave_document_chunks\ncreate_document_chunks\nlist_document_chunks\ndelete_document_chunks\n你自己先写\n先不要写上传接口。\n\n先完成：\n\npaths.py\nschemas.py\ndocument_store.py\n然后用 Python 交互测试 store 是否能创建记录。\n\n代码骨架\npaths.py\nfrom pathlib import Path\n\nBASE_ABS_DIR = Path(__file__).resolve().parent\n\nSTATIC_DIR = BASE_ABS_DIR / \"static\"\nLOG_DIR = BASE_ABS_DIR / \"logs\"\nDATA_DIR = BASE_ABS_DIR / \"data\"\nUPLOAD_DIR = DATA_DIR / \"uploads\"\n\nDOCUMENTS_FILE = DATA_DIR / \"documents.json\"\nDOCUMENT_CHUNKS_FILE = DATA_DIR / \"document_chunks.json\"\n\nWEB_DIR = BASE_ABS_DIR.parents[1] / \"web\"\nschemas.py 骨架\nclass DocumentItem(BaseModel):\n    ...\n\n\nclass DocumentDetailResponse(DocumentItem):\n    ...\n\n\nclass DocumentListResponse(BaseModel):\n    ...\n\n\nclass UploadDocumentResponse(BaseModel):\n    ...\n\n\nclass DocumentChunkItem(BaseModel):\n    ...\n\n\nclass DocumentChunkListResponse(BaseModel):\n    ...\n\n\nclass DeleteDocumentResponse(BaseModel):\n    ...\ndocument_store.py 骨架\nimport json\nimport uuid\nfrom datetime import datetime\nfrom typing import Any\n\nfrom src.app.exceptions import AppError\nfrom src.app.paths import DOCUMENTS_FILE, DOCUMENT_CHUNKS_FILE, DATA_DIR\n\n\ndef _now_iso() -> str:\n    ...\n\n\ndef _ensure_json_file(path) -> None:\n    ...\n\n\ndef _load_json_list(path) -> list[dict[str, Any]]:\n    ...\n\n\ndef _save_json_list(path, items: list[dict[str, Any]]) -> None:\n    ...\n\n\ndef load_documents() -> list[dict[str, Any]]:\n    ...\n\n\ndef save_documents(documents: list[dict[str, Any]]) -> None:\n    ...\n\n\ndef create_document(...) -> dict[str, Any]:\n    ...\n\n\ndef update_document(document_id: str, updates: dict[str, Any]) -> dict[str, Any]:\n    ...\n\n\ndef get_document(document_id: str) -> dict[str, Any] | None:\n    ...\n\n\ndef list_documents() -> list[dict[str, Any]]:\n    ...\n\n\ndef delete_document(document_id: str) -> None:\n    ...\n\n\ndef load_document_chunks() -> list[dict[str, Any]]:\n    ...\n\n\ndef save_document_chunks(chunks: list[dict[str, Any]]) -> None:\n    ...\n\n\ndef create_document_chunks(document_id: str, chunks: list[dict[str, Any]]) -> list[dict[str, Any]]:\n    ...\n\n\ndef list_document_chunks(document_id: str) -> list[dict[str, Any]]:\n    ...\n\n\ndef delete_document_chunks(document_id: str) -> None:\n    ...\n检查点\n完成后确认：\n\n1. paths.py 有 UPLOAD_DIR、DOCUMENTS_FILE、DOCUMENT_CHUNKS_FILE\n2. schemas.py 没有拆文件\n3. document_store.py 没塞进 conversation_store.py\n4. JSON 写入使用 ensure_ascii=False\n5. created_at / updated_at 使用 datetime.now().isoformat()\n6. document id 使用 uuid\n7. chunk id 使用 uuid\n8. chunk_index 由 create_document_chunks 保证连续\n参考实现：schemas.py 新增部分\n把下面代码追加到 src/app/schemas.py 末尾即可。\n\nclass DocumentItem(BaseModel):\n    id: str\n    filename: str\n    file_type: str\n    source_path: str\n    status: str\n    chunk_count: int\n    char_count: int\n    error_message: str | None = None\n    created_at: str\n    updated_at: str\n\n\nclass DocumentDetailResponse(DocumentItem):\n    content: str\n\n\nclass DocumentListResponse(BaseModel):\n    items: list[DocumentItem]\n\n\nclass UploadDocumentResponse(BaseModel):\n    document_id: str\n    filename: str\n    file_type: str\n    status: str\n    chunk_count: int\n    char_count: int\n    created_at: str\n\n\nclass DocumentChunkItem(BaseModel):\n    id: str\n    document_id: str\n    chunk_index: int\n    heading: str | None = None\n    content: str\n    char_count: int\n    estimated_tokens: int\n    metadata: dict[str, Any] = Field(default_factory=dict)\n    created_at: str\n\n\nclass DocumentChunkListResponse(BaseModel):\n    items: list[DocumentChunkItem]\n\n\nclass DeleteDocumentResponse(BaseModel):\n    success: bool\n    message: str\n字段逐项解释\nDocumentItem.id\n文档唯一 ID。\n\n不要用 filename 当 ID，因为：\n\n文件名可能重复\n文件名可能包含危险字符\n文件名可能被用户改动\nfilename\n用户上传时的原始文件名，主要用于展示。\n\nsource_path\n服务端保存后的真实路径。\n\n它和 filename 不同：\n\nfilename: fastapi-guide.md\nsource_path: src/app/data/uploads/uuid_fastapi-guide.md\nsource_path 后续用于：\n\n排查上传文件\n重建索引\n重新解析\n删除原始文件\nfile_type\n建议保存为：\n\nmd\ntxt\n不要只在每次使用时重新从 filename 猜。\n\n因为后续文件名可能不可信，业务处理应该依赖系统校验后的结果。\n\nstatus\n文档状态：\n\npending\nprocessing\ncompleted\nfailed\n真实系统中上传解析经常变成异步任务，状态字段是后续扩展基础。\n\nerror_message\n失败时记录原因。\n\n例如：\n\n文件类型不支持\n文件过大\n文本解码失败\nchunk 为空\ncontent\n原始文本内容。\n\n只在详情接口返回，不建议列表接口返回。\n\n原因：\n\n列表接口可能很多文档\ncontent 可能很长\n影响响应性能\n前端列表也不需要全文\nchunk_index\n同一个 document 下的 chunk 顺序。\n\n必须稳定连续：\n\n0, 1, 2, 3...\n后续用于：\n\n调试\n上下文重组\n前端预览\n引用排序\nheading\nMarkdown 标题。\n\n它能保留文档结构，让 chunk 更可解释。\n\n例如：\n\nheading = JSONResponse\ncontent = JSONResponse 可以自定义异常返回结构...\n比单独 content 更容易理解来源。\n\nchar_count\n字符数。\n\n用于：\n\n检查 chunk 是否过长\n控制切分策略\n调试异常 chunk\nestimated_tokens\n粗略 token 估算。\n\n和第四周 ContextBuilder 的思想一致，但不要直接依赖 ContextBuilder。\n\n因为：\n\nContextBuilder 属于会话上下文\nChunkSplitter 属于文档处理\n两个模块职责不同\nmetadata\n后续非常重要。\n\n可以保存：\n\n{\n  \"split_method\": \"markdown_heading\",\n  \"file_type\": \"md\",\n  \"heading_level\": 2\n}\n后续 RAG 可以用它做：\n\n过滤\n溯源\n展示\n评测\n调试\n参考实现：document_store.py\nimport json\nimport uuid\nfrom datetime import datetime\nfrom pathlib import Path\nfrom typing import Any\n\nfrom src.app.exceptions import AppError\nfrom src.app.paths import DATA_DIR, DOCUMENTS_FILE, DOCUMENT_CHUNKS_FILE\n\n\ndef _now_iso() -> str:\n    return datetime.now().isoformat()\n\n\ndef _ensure_json_file(path: Path) -> None:\n    DATA_DIR.mkdir(parents=True, exist_ok=True)\n\n    if not path.exists():\n        path.write_text(\"[]\", encoding=\"utf-8\")\n\n\ndef _load_json_list(path: Path) -> list[dict[str, Any]]:\n    _ensure_json_file(path)\n\n    try:\n        content = path.read_text(encoding=\"utf-8\").strip()\n        if not content:\n            return []\n\n        data = json.loads(content)\n\n        if not isinstance(data, list):\n            raise AppError(\n                code=\"DOCUMENT_STORE_ERROR\",\n                message=\"JSON 存储格式错误\",\n                detail=f\"{path} 内容不是数组\",\n                status_code=500,\n            )\n\n        return data\n\n    except json.JSONDecodeError as exc:\n        raise AppError(\n            code=\"DOCUMENT_STORE_ERROR\",\n            message=\"读取 JSON 失败\",\n            detail=f\"{path}: {exc}\",\n            status_code=500,\n        ) from exc\n\n\ndef _save_json_list(path: Path, items: list[dict[str, Any]]) -> None:\n    _ensure_json_file(path)\n\n    try:\n        path.write_text(\n            json.dumps(items, ensure_ascii=False, indent=2),\n            encoding=\"utf-8\",\n        )\n    except OSError as exc:\n        raise AppError(\n            code=\"DOCUMENT_STORE_ERROR\",\n            message=\"写入 JSON 失败\",\n            detail=f\"{path}: {exc}\",\n            status_code=500,\n        ) from exc\n\n\ndef load_documents() -> list[dict[str, Any]]:\n    return _load_json_list(DOCUMENTS_FILE)\n\n\ndef save_documents(documents: list[dict[str, Any]]) -> None:\n    _save_json_list(DOCUMENTS_FILE, documents)\n\n\ndef create_document(\n    filename: str,\n    file_type: str,\n    source_path: str,\n    content: str,\n    status: str = \"pending\",\n    error_message: str | None = None,\n) -> dict[str, Any]:\n    now = _now_iso()\n\n    document = {\n        \"id\": str(uuid.uuid4()),\n        \"filename\": filename,\n        \"file_type\": file_type,\n        \"source_path\": source_path,\n        \"content\": content,\n        \"status\": status,\n        \"chunk_count\": 0,\n        \"char_count\": len(content),\n        \"error_message\": error_message,\n        \"created_at\": now,\n        \"updated_at\": now,\n    }\n\n    documents = load_documents()\n    documents.append(document)\n    save_documents(documents)\n\n    return document\n\n\ndef update_document(\n    document_id: str,\n    updates: dict[str, Any],\n) -> dict[str, Any]:\n    documents = load_documents()\n\n    for document in documents:\n        if document[\"id\"] == document_id:\n            document.update(updates)\n            document[\"updated_at\"] = _now_iso()\n            save_documents(documents)\n            return document\n\n    raise AppError(\n        code=\"DOCUMENT_NOT_FOUND\",\n        message=\"文档不存在\",\n        detail=f\"document_id={document_id}\",\n        status_code=404,\n    )\n\n\ndef get_document(document_id: str) -> dict[str, Any] | None:\n    documents = load_documents()\n\n    for document in documents:\n        if document[\"id\"] == document_id:\n            return document\n\n    return None\n\n\ndef list_documents() -> list[dict[str, Any]]:\n    documents = load_documents()\n    return sorted(\n        documents,\n        key=lambda item: item.get(\"updated_at\", \"\"),\n        reverse=True,\n    )\n\n\ndef delete_document(document_id: str) -> None:\n    documents = load_documents()\n    next_documents = [\n        document for document in documents if document[\"id\"] != document_id\n    ]\n\n    if len(next_documents) == len(documents):\n        raise AppError(\n            code=\"DOCUMENT_NOT_FOUND\",\n            message=\"文档不存在\",\n            detail=f\"document_id={document_id}\",\n            status_code=404,\n        )\n\n    save_documents(next_documents)\n\n\ndef load_document_chunks() -> list[dict[str, Any]]:\n    return _load_json_list(DOCUMENT_CHUNKS_FILE)\n\n\ndef save_document_chunks(chunks: list[dict[str, Any]]) -> None:\n    _save_json_list(DOCUMENT_CHUNKS_FILE, chunks)\n\n\ndef create_document_chunks(\n    document_id: str,\n    chunks: list[dict[str, Any]],\n) -> list[dict[str, Any]]:\n    now = _now_iso()\n\n    records: list[dict[str, Any]] = []\n\n    for index, chunk in enumerate(chunks):\n        content = chunk.get(\"content\", \"\").strip()\n        if not content:\n            continue\n\n        record = {\n            \"id\": str(uuid.uuid4()),\n            \"document_id\": document_id,\n            \"chunk_index\": index,\n            \"heading\": chunk.get(\"heading\"),\n            \"content\": content,\n            \"char_count\": len(content),\n            \"estimated_tokens\": int(chunk.get(\"estimated_tokens\") or 0),\n            \"metadata\": chunk.get(\"metadata\") or {},\n            \"created_at\": now,\n        }\n        records.append(record)\n\n    existing_chunks = load_document_chunks()\n    existing_chunks.extend(records)\n    save_document_chunks(existing_chunks)\n\n    return records\n\n\ndef list_document_chunks(document_id: str) -> list[dict[str, Any]]:\n    chunks = load_document_chunks()\n    document_chunks = [\n        chunk for chunk in chunks if chunk[\"document_id\"] == document_id\n    ]\n\n    return sorted(\n        document_chunks,\n        key=lambda item: item.get(\"chunk_index\", 0),\n    )\n\n\ndef delete_document_chunks(document_id: str) -> None:\n    chunks = load_document_chunks()\n    next_chunks = [\n        chunk for chunk in chunks if chunk[\"document_id\"] != document_id\n    ]\n    save_document_chunks(next_chunks)\n逐行解释\n_ensure_json_file\nDATA_DIR.mkdir(parents=True, exist_ok=True)\n确保数据目录存在。\n\n这延续你 conversation_store.py 的风格。\n\n_load_json_list\n负责读取 JSON 数组。\n\n如果文件为空，返回空列表。\n\n如果 JSON 不是数组，抛出 AppError。\n\n_save_json_list\njson.dumps(items, ensure_ascii=False, indent=2)\nensure_ascii=False 很重要。\n\n否则中文会变成：\n\n\\u5feb\\u901f\\u5165\\u95e8\n不利于你直接打开 JSON 调试。\n\ncreate_document\n创建 document 主记录。\n\n注意此时 chunk_count = 0，因为 chunk 还没有生成。\n\nupdate_document\n用于更新状态：\n\npending -> processing -> completed\npending -> processing -> failed\ncreate_document_chunks\n它接收 splitter 产生的 chunk 草稿，然后补齐：\n\nid\ndocument_id\nchunk_index\ncreated_at\n这说明：\n\nsplitter 只负责切文本\nstore 负责存储字段\n边界清晰。\n\n测试命令\npython -m compileall src/app\n解释：\n\npython -m compileall：让 Python 编译检查源码\nsrc/app：检查后端源码目录\n用 uv：\n\nuv run python -m compileall src/app\n常见错误\n1. ImportError: cannot import name DOCUMENTS_FILE\n说明 paths.py 还没有新增：\n\nDOCUMENTS_FILE = DATA_DIR / \"documents.json\"\n2. DOCUMENT_STORE_ERROR\n打开对应 JSON 文件检查是否不是数组。\n\n正确格式：\n\n[]\n错误格式：\n\n{}\n复盘问题\n1. 为什么 document_store.py 不应该和 conversation_store.py 合并？\n2. 为什么 Document 要有 status？\n3. 为什么 content 不放在列表接口里返回？\n4. 为什么 chunk_index 必须稳定连续？\n可面试表达\n我在第五周先没有直接做 RAG，而是先实现了知识库文档处理层。Document 作为一级资源保存原始文件、解析文本、处理状态和错误信息；DocumentChunk 作为后续 Embedding 和 RAG 检索的最小语义单元。当前阶段继续用 JSON Store，主要是为了保持学习路径清晰和结果可观察，后续 Week 6 再切到 PostgreSQL / pgvector。\nDay 2：实现 DocumentLoader，支持 md/txt 上传和文本读取\n目标\n今天完成：\n\n1. 新增 services/document_loader.py\n2. 校验文件后缀\n3. 校验文件大小\n4. 保存原始上传文件\n5. 读取 utf-8 文本\n6. 明确不支持 PDF\n核心思想\nDocumentLoader 是 AI 数据入口。\n\n它负责把外部上传文件变成系统内部可信的文本。\n\n为什么\n用户上传的文件是不可信的。\n\n你不能相信：\n\nfilename 一定安全\n后缀一定真实\n文件一定不大\n文件一定是 utf-8\n文件一定有内容\n所以 Loader 必须做：\n\nvalidate\nsave\nload text\n这和 Node.js 的上传处理很像：\n\nmulter 接收文件\n校验 mimetype / ext / size\n保存到 uploads\n再交给 service 处理\n怎么做\n创建文件：\n\nsrc/app/services/document_loader.py\n设计：\n\nALLOWED_DOCUMENT_EXTENSIONS = {\".md\", \".txt\"}\nMAX_DOCUMENT_FILE_SIZE_BYTES = 5 * 1024 * 1024\n\nclass DocumentLoader:\n    def validate_file(self, filename: str, content: bytes) -> str:\n        ...\n\n    def save_upload_file(self, filename: str, content: bytes) -> Path:\n        ...\n\n    def load_text(self, filename: str, content: bytes) -> str:\n        ...\n你自己先写\n先实现三个函数：\n\nvalidate_file\nsave_upload_file\nload_text\n暂时不要管 API。\n\n代码骨架\nimport re\nimport uuid\nfrom pathlib import Path\n\nfrom src.app.exceptions import AppError\nfrom src.app.paths import UPLOAD_DIR\n\nALLOWED_DOCUMENT_EXTENSIONS = {\".md\", \".txt\"}\nMAX_DOCUMENT_FILE_SIZE_BYTES = 5 * 1024 * 1024\n\n\nclass DocumentLoader:\n    def validate_file(self, filename: str, content: bytes) -> str:\n        ...\n\n    def save_upload_file(self, filename: str, content: bytes) -> Path:\n        ...\n\n    def load_text(self, filename: str, content: bytes) -> str:\n        ...\n\n    def _safe_filename(self, filename: str) -> str:\n        ...\n检查点\n完成后确认：\n\n1. 只允许 .md / .txt\n2. 空文件报错\n3. 超过 5MB 报错\n4. utf-8 解码失败报错\n5. 保存文件名有 uuid 前缀\n6. 不直接相信用户 filename\n7. 保存目录是 UPLOAD_DIR\n8. 返回 file_type 是 md/txt，不带点\n参考实现：document_loader.py\nimport re\nimport uuid\nfrom pathlib import Path\n\nfrom src.app.exceptions import AppError\nfrom src.app.paths import UPLOAD_DIR\n\nALLOWED_DOCUMENT_EXTENSIONS = {\".md\", \".txt\"}\nMAX_DOCUMENT_FILE_SIZE_BYTES = 5 * 1024 * 1024\n\n\nclass DocumentLoader:\n    def validate_file(self, filename: str, content: bytes) -> str:\n        if not filename:\n            raise AppError(\n                code=\"DOCUMENT_INVALID_FILE\",\n                message=\"文件名不能为空\",\n                status_code=400,\n            )\n\n        suffix = Path(filename).suffix.lower()\n\n        if suffix not in ALLOWED_DOCUMENT_EXTENSIONS:\n            raise AppError(\n                code=\"DOCUMENT_UNSUPPORTED_FILE_TYPE\",\n                message=\"暂只支持 .md 和 .txt 文件\",\n                detail=f\"filename={filename}\",\n                status_code=400,\n            )\n\n        if not content:\n            raise AppError(\n                code=\"DOCUMENT_EMPTY_FILE\",\n                message=\"上传文件不能为空\",\n                status_code=400,\n            )\n\n        if len(content) > MAX_DOCUMENT_FILE_SIZE_BYTES:\n            raise AppError(\n                code=\"DOCUMENT_FILE_TOO_LARGE\",\n                message=\"上传文件过大\",\n                detail=f\"max_size_bytes={MAX_DOCUMENT_FILE_SIZE_BYTES}\",\n                status_code=400,\n            )\n\n        return suffix.lstrip(\".\")\n\n    def save_upload_file(self, filename: str, content: bytes) -> Path:\n        UPLOAD_DIR.mkdir(parents=True, exist_ok=True)\n\n        safe_name = self._safe_filename(filename)\n        saved_name = f\"{uuid.uuid4()}_{safe_name}\"\n        saved_path = UPLOAD_DIR / saved_name\n\n        saved_path.write_bytes(content)\n\n        return saved_path\n\n    def load_text(self, filename: str, content: bytes) -> str:\n        self.validate_file(filename=filename, content=content)\n\n        try:\n            text = content.decode(\"utf-8\")\n        except UnicodeDecodeError as exc:\n            raise AppError(\n                code=\"DOCUMENT_DECODE_ERROR\",\n                message=\"文件文本解码失败，请确认文件是 UTF-8 编码\",\n                detail=str(exc),\n                status_code=400,\n            ) from exc\n\n        text = text.strip()\n\n        if not text:\n            raise AppError(\n                code=\"DOCUMENT_EMPTY_TEXT\",\n                message=\"文件解析后文本为空\",\n                status_code=400,\n            )\n\n        return text\n\n    def _safe_filename(self, filename: str) -> str:\n        name = Path(filename).name\n        return re.sub(r\"[^a-zA-Z0-9._\\-\\u4e00-\\u9fff]\", \"_\", name)\n逐行解释\nALLOWED_DOCUMENT_EXTENSIONS\n{\".md\", \".txt\"}\n第五周只支持纯文本类文件。\n\nMAX_DOCUMENT_FILE_SIZE_BYTES\n5 * 1024 * 1024\n限制 5MB。\n\n为什么要限制？\n\n防止内存占用过大\n防止恶意上传\n防止 JSON 文件膨胀\n防止同步接口阻塞太久\nvalidate_file\n返回：\n\nmd\ntxt\n而不是：\n\n.md\n.txt\n这样 API 响应更像业务字段。\n\nPath(filename).suffix.lower()\n统一处理大小写：\n\nREADME.MD -> .md\nnote.TXT -> .txt\nsave_upload_file\nsaved_name = f\"{uuid.uuid4()}_{safe_name}\"\n为什么加 uuid？\n\n避免同名覆盖\n降低路径猜测风险\n便于追踪具体上传批次\n_safe_filename\n用户传来的 filename 可能包含：\n\n../\n空格\n特殊字符\n奇怪符号\n所以要安全化。\n\nload_text\n.md 和 .txt 本质上都是纯文本。\n\n本周不做 Markdown AST 解析，只读取文本内容。\n\nUploadFile 解释\nFastAPI 的 UploadFile 是文件上传对象。\n\n上传接口中通常这样用：\n\nasync def upload(file: UploadFile = File(...)):\n    content = await file.read()\n为什么 await file.read() 返回 bytes\n因为 HTTP 上传的是二进制流。\n\n即使 .md / .txt 是文本，网络层接收时也是 bytes。\n\n你需要：\n\ncontent.decode(\"utf-8\")\n才能变成 Python 字符串。\n\n为什么处理 UnicodeDecodeError\n因为用户可能上传：\n\nGBK 编码 txt\n损坏文件\n伪装成 txt 的二进制文件\n第五周先明确要求 UTF-8。\n\n测试命令\npython -m compileall src/app\n临时 Python 测试：\n\npython\nfrom src.app.services.document_loader import DocumentLoader\n\nloader = DocumentLoader()\ncontent = \"# 标题\\n\\n这是内容\".encode(\"utf-8\")\n\nfile_type = loader.validate_file(\"test.md\", content)\ntext = loader.load_text(\"test.md\", content)\npath = loader.save_upload_file(\"test.md\", content)\n\nprint(file_type)\nprint(text)\nprint(path)\n预期：\n\nmd\n# 标题\n\n这是内容\nsrc/app/data/uploads/xxx_test.md\n常见错误\n1. DOCUMENT_UNSUPPORTED_FILE_TYPE\n说明后缀不是 .md / .txt。\n\n2. DOCUMENT_DECODE_ERROR\n说明不是 UTF-8 文本。\n\n先用：\n\nfile /tmp/your-file.txt\n检查编码。\n\n3. 找不到 uploads\n检查 paths.py 是否有：\n\nUPLOAD_DIR = DATA_DIR / \"uploads\"\n复盘问题\n1. UploadFile 为什么读出来是 bytes？\n2. filename 和 source_path 为什么不能混用？\n3. 为什么上传文件要限制大小？\n4. 为什么第五周不支持 PDF？\n可面试表达\n我把文档上传入口单独封装为 DocumentLoader。它负责校验文件后缀、大小、UTF-8 解码和原始文件保存，避免外部上传数据直接进入业务处理。第一版只支持 md/txt，因为这两个格式本质是纯文本，可以聚焦 document -> text -> chunk 的 RAG 前置管线，PDF 后续单独作为解析专题处理。\nDay 3：实现 ChunkSplitter，按标题、段落、长度切分\n目标\n今天完成第五周最核心模块：\n\nsrc/app/services/chunk_splitter.py\n能力：\n\n1. Markdown 标题识别\n2. Markdown section 切分\n3. txt 段落切分\n4. 超长文本按字符长度兜底切分\n5. 支持 overlap\n6. 支持 heading\n7. 支持 char_count\n8. 支持 estimated_tokens\n9. 支持 metadata\n核心思想\nChunk 不是随便切字符串。\n\nChunk 是：\n\n后续 Embedding 和 RAG 检索的最小语义单元。\n为什么\n后续 Week 6 会把每个 chunk 转成 embedding。\n\n如果 chunk 切分不好，会出现：\n\n召回不到正确内容\n召回内容太散\n模型回答缺少依据\nsources 不可解释\n为什么不能无脑每 500 字切一段\n例如 Markdown：\n\n# FastAPI 异常处理\n\n...\n\n## JSONResponse\n\n...\n如果固定 500 字切，可能把两个标题混在一起。\n\n更好的方式是先尊重文档结构：\n\n标题 -> section -> paragraph -> length fallback\n为什么需要 overlap\n假设有一段内容：\n\nFastAPI 的异常处理通过 exception_handler 注册。\n它可以捕获自定义业务异常 AppError。\n如果刚好从中间切开，第二个 chunk 可能丢失上下文。\n\noverlap 可以让相邻 chunk 保留一点重叠文本，降低边界信息丢失。\n\noverlap 太大的坏处\n重复内容太多\nembedding 成本增加\n检索结果冗余\nJSON 存储膨胀\n第五周建议：\n\nCHUNK_MAX_CHARS = 1000\nCHUNK_OVERLAP_CHARS = 120\nMIN_CHUNK_CHARS = 80\n怎么做\n新增：\n\nsrc/app/services/chunk_splitter.py\n设计：\n\nclass ChunkSplitter:\n    def split(self, text: str, file_type: str) -> list[dict[str, Any]]:\n        ...\n\n    def split_markdown(self, text: str) -> list[dict[str, Any]]:\n        ...\n\n    def split_plain_text(self, text: str) -> list[dict[str, Any]]:\n        ...\n\n    def split_long_text(self, text: str, heading: str | None = None) -> list[dict[str, Any]]:\n        ...\n\n    def estimate_tokens(self, text: str) -> int:\n        ...\n你自己先写\n先写 estimate_tokens 和 split_long_text。\n\n再写 txt 段落切分。\n\n最后写 Markdown 标题识别。\n\n代码骨架\nimport re\nfrom typing import Any\n\nCHUNK_MAX_CHARS = 1000\nCHUNK_OVERLAP_CHARS = 120\nMIN_CHUNK_CHARS = 80\n\n\nclass ChunkSplitter:\n    def split(self, text: str, file_type: str) -> list[dict[str, Any]]:\n        ...\n\n    def split_markdown(self, text: str) -> list[dict[str, Any]]:\n        ...\n\n    def split_plain_text(self, text: str) -> list[dict[str, Any]]:\n        ...\n\n    def split_long_text(self, text: str, heading: str | None = None) -> list[dict[str, Any]]:\n        ...\n\n    def estimate_tokens(self, text: str) -> int:\n        ...\n\n    def _build_chunk(...):\n        ...\n检查点\n1. splitter 不访问 JSON\n2. splitter 不调用 LLM\n3. splitter 不知道 document_id\n4. Markdown heading 能保留\n5. txt heading 为 None\n6. chunk content 不为空\n7. chunk 太长会兜底切\n8. estimated_tokens 合理\n9. metadata 标记 split_method\n10. 示例 Markdown 应生成 4 个 chunk\n参考实现：chunk_splitter.py\nimport re\nfrom typing import Any\n\nCHUNK_MAX_CHARS = 1000\nCHUNK_OVERLAP_CHARS = 120\nMIN_CHUNK_CHARS = 80\n\n\nclass ChunkSplitter:\n    def split(self, text: str, file_type: str) -> list[dict[str, Any]]:\n        normalized_text = text.strip()\n\n        if not normalized_text:\n            return []\n\n        if file_type == \"md\":\n            return self.split_markdown(normalized_text)\n\n        if file_type == \"txt\":\n            return self.split_plain_text(normalized_text)\n\n        return self.split_plain_text(normalized_text)\n\n    def split_markdown(self, text: str) -> list[dict[str, Any]]:\n        heading_pattern = re.compile(r\"^(#{1,6})\\s+(.+)$\", re.MULTILINE)\n\n        matches = list(heading_pattern.finditer(text))\n\n        if not matches:\n            return self.split_plain_text(text)\n\n        chunks: list[dict[str, Any]] = []\n\n        for index, match in enumerate(matches):\n            heading = match.group(2).strip()\n            heading_level = len(match.group(1))\n\n            section_start = match.end()\n            section_end = matches[index + 1].start() if index + 1 < len(matches) else len(text)\n\n            section_content = text[section_start:section_end].strip()\n\n            if not section_content:\n                continue\n\n            section_chunks = self._split_by_paragraphs(\n                text=section_content,\n                heading=heading,\n                metadata={\n                    \"split_method\": \"markdown_heading\",\n                    \"heading_level\": heading_level,\n                },\n            )\n\n            chunks.extend(section_chunks)\n\n        return chunks\n\n    def split_plain_text(self, text: str) -> list[dict[str, Any]]:\n        return self._split_by_paragraphs(\n            text=text,\n            heading=None,\n            metadata={\n                \"split_method\": \"paragraph\",\n            },\n        )\n\n    def _split_by_paragraphs(\n        self,\n        text: str,\n        heading: str | None,\n        metadata: dict[str, Any],\n    ) -> list[dict[str, Any]]:\n        paragraphs = [\n            paragraph.strip()\n            for paragraph in re.split(r\"\\n\\s*\\n\", text)\n            if paragraph.strip()\n        ]\n\n        chunks: list[dict[str, Any]] = []\n        buffer: list[str] = []\n        buffer_chars = 0\n\n        for paragraph in paragraphs:\n            paragraph_len = len(paragraph)\n\n            if paragraph_len > CHUNK_MAX_CHARS:\n                if buffer:\n                    chunks.append(\n                        self._build_chunk(\n                            content=\"\\n\\n\".join(buffer),\n                            heading=heading,\n                            metadata=metadata,\n                        )\n                    )\n                    buffer = []\n                    buffer_chars = 0\n\n                chunks.extend(\n                    self.split_long_text(\n                        text=paragraph,\n                        heading=heading,\n                        metadata={\n                            **metadata,\n                            \"split_method\": \"length_fallback\",\n                        },\n                    )\n                )\n                continue\n\n            next_chars = buffer_chars + paragraph_len\n\n            if buffer and next_chars > CHUNK_MAX_CHARS:\n                chunks.append(\n                    self._build_chunk(\n                        content=\"\\n\\n\".join(buffer),\n                        heading=heading,\n                        metadata=metadata,\n                    )\n                )\n                buffer = [paragraph]\n                buffer_chars = paragraph_len\n            else:\n                buffer.append(paragraph)\n                buffer_chars = next_chars\n\n        if buffer:\n            chunks.append(\n                self._build_chunk(\n                    content=\"\\n\\n\".join(buffer),\n                    heading=heading,\n                    metadata=metadata,\n                )\n            )\n\n        return [\n            chunk\n            for chunk in chunks\n            if len(chunk[\"content\"]) >= MIN_CHUNK_CHARS or len(chunks) == 1\n        ]\n\n    def split_long_text(\n        self,\n        text: str,\n        heading: str | None = None,\n        metadata: dict[str, Any] | None = None,\n    ) -> list[dict[str, Any]]:\n        chunks: list[dict[str, Any]] = []\n\n        start = 0\n        text_length = len(text)\n        step = max(1, CHUNK_MAX_CHARS - CHUNK_OVERLAP_CHARS)\n\n        while start < text_length:\n            end = min(start + CHUNK_MAX_CHARS, text_length)\n            content = text[start:end].strip()\n\n            if content:\n                chunks.append(\n                    self._build_chunk(\n                        content=content,\n                        heading=heading,\n                        metadata=metadata or {\"split_method\": \"length_fallback\"},\n                    )\n                )\n\n            if end >= text_length:\n                break\n\n            start += step\n\n        return chunks\n\n    def estimate_tokens(self, text: str) -> int:\n        if not text:\n            return 0\n\n        chinese_chars = sum(1 for ch in text if \"\\u4e00\" <= ch <= \"\\u9fff\")\n        other_chars = len(text) - chinese_chars\n\n        return chinese_chars + max(1, other_chars // 4)\n\n    def _build_chunk(\n        self,\n        content: str,\n        heading: str | None,\n        metadata: dict[str, Any],\n    ) -> dict[str, Any]:\n        normalized_content = content.strip()\n\n        return {\n            \"heading\": heading,\n            \"content\": normalized_content,\n            \"char_count\": len(normalized_content),\n            \"estimated_tokens\": self.estimate_tokens(normalized_content),\n            \"metadata\": metadata,\n        }\n逐行解释\nsplit\n统一入口。\n\n根据 file_type 分发：\n\nmd -> split_markdown\ntxt -> split_plain_text\nheading_pattern\nr\"^(#{1,6})\\s+(.+)$\"\n匹配 Markdown 标题：\n\n# 一级标题\n## 二级标题\n### 三级标题\nmatch.group(2)\n取标题文字。\n\n例如：\n\n## JSONResponse\n得到：\n\nJSONResponse\nsection_start / section_end\n用于截取当前标题到下一个标题之间的内容。\n\n_split_by_paragraphs\n段落比固定长度更自然。\n\n段落通常代表一个较完整语义单元。\n\nsplit_long_text\n兜底切分。\n\n当单个段落超过 CHUNK_MAX_CHARS，必须强制拆开，否则后续 embedding 太长。\n\nstep = CHUNK_MAX_CHARS - CHUNK_OVERLAP_CHARS\n例如：\n\nmax = 1000\noverlap = 120\nstep = 880\n第二个 chunk 会从第 880 个字符开始，和上一个 chunk 重叠 120 字符。\n\nMIN_CHUNK_CHARS\n过滤极短 chunk。\n\n但如果全文只有一个 chunk，即使小于 80 字，也保留。\n\n测试命令\n临时测试：\n\npython\nfrom src.app.services.chunk_splitter import ChunkSplitter\n\ntext = \"\"\"# FastAPI 异常处理\n\nFastAPI 可以通过 exception_handler 注册全局异常处理器。\n\n## AppError\n\nAppError 是我们自定义的业务异常基类。\n\n## JSONResponse\n\nJSONResponse 可以自定义异常返回的 JSON 结构。\n\n## RequestValidationError\n\nRequestValidationError 用来处理请求参数验证失败。\n\"\"\"\n\nsplitter = ChunkSplitter()\nchunks = splitter.split(text, \"md\")\n\nfor chunk in chunks:\n    print(chunk[\"heading\"], chunk[\"content\"], chunk[\"char_count\"])\n期望：\n\nFastAPI 异常处理 ... exception_handler ...\nAppError ... 业务异常基类 ...\nJSONResponse ... JSON 结构 ...\nRequestValidationError ... 请求参数验证失败 ...\n常见错误\n1. Markdown 没切出标题\n检查正则：\n\nr\"^(#{1,6})\\s+(.+)$\"\n必须有 re.MULTILINE。\n\n2. chunk 为空\n检查标题下面是否有正文。\n\n本策略会跳过只有标题、没有内容的 section。\n\n3. 只生成 1 个 chunk\n可能原因：\n\n文件不是 md\n标题格式没有空格\n文本太短\n正确标题：\n\n## AppError\n错误标题：\n\n##AppError\n复盘问题\n1. 为什么 chunk 不是普通字符串切片？\n2. 为什么 Markdown 标题很重要？\n3. 为什么 paragraph 比固定长度更自然？\n4. overlap 解决什么问题？\n5. splitter 为什么不能访问数据库？\n可面试表达\nChunkSplitter 是 RAG 前置管线里的关键模块。我的第一版策略不是固定长度硬切，而是优先尊重 Markdown 标题结构，再按段落合并，最后对超长段落用字符长度兜底切分，并保留 heading、char_count、estimated_tokens 和 metadata。这能让后续 embedding 更接近语义单元，也让 RAG sources 更可解释。\nDay 4：实现 DocumentService，串起上传、解析、切分、保存\n目标\n今天完成：\n\nsrc/app/services/document_service.py\n它负责：\n\n1. 接收 UploadFile\n2. 调用 DocumentLoader\n3. 创建 document\n4. 调用 ChunkSplitter\n5. 保存 chunks\n6. 更新 document 状态\n7. 查询 list/detail/chunks\n8. 删除 document\n核心思想\nDocumentService 是编排层。\n\n它不亲自解析文件，不亲自切分文本，不亲自操作 JSON 细节，而是组织它们。\n\n为什么\n如果你把所有逻辑都写在一个函数里，会变成：\n\n读取 UploadFile\n校验后缀\n校验大小\n保存文件\ndecode utf-8\n创建 document\n正则切 markdown\n切 paragraph\n写 chunks json\n处理错误\n返回响应\n这就是巨型函数。\n\n真实工程要拆成：\n\nDocumentLoader：外部文件 -> text\nChunkSplitter：text -> chunks\ndocument_store：dict -> JSON\nDocumentService：编排业务流程\nroutes.py：HTTP 参数接收和响应包装\n类比 NestJS：\n\nController -> Service -> Repository\n怎么做\n新增：\n\nsrc/app/services/document_service.py\n设计：\n\nclass DocumentService:\n    async def upload_document(self, file: UploadFile) -> dict[str, Any]:\n        ...\n\n    def list_documents(self) -> list[dict[str, Any]]:\n        ...\n\n    def get_document_detail(self, document_id: str) -> dict[str, Any]:\n        ...\n\n    def get_document_chunks(self, document_id: str) -> list[dict[str, Any]]:\n        ...\n\n    def delete_document(self, document_id: str) -> dict[str, Any]:\n        ...\n你自己先写\n先写查询函数：\n\nlist_documents\nget_document_detail\nget_document_chunks\n再写上传主流程。\n\n最后写 delete。\n\n代码骨架\nfrom pathlib import Path\nfrom typing import Any\n\nfrom fastapi import UploadFile\n\nfrom src.app.document_store import ...\nfrom src.app.exceptions import AppError\nfrom src.app.services.chunk_splitter import ChunkSplitter\nfrom src.app.services.document_loader import DocumentLoader\n\n\nclass DocumentService:\n    def __init__(self) -> None:\n        self.loader = DocumentLoader()\n        self.splitter = ChunkSplitter()\n\n    async def upload_document(self, file: UploadFile) -> dict[str, Any]:\n        ...\n\n    def list_documents(self) -> list[dict[str, Any]]:\n        ...\n\n    def get_document_detail(self, document_id: str) -> dict[str, Any]:\n        ...\n\n    def get_document_chunks(self, document_id: str) -> list[dict[str, Any]]:\n        ...\n\n    def delete_document(self, document_id: str) -> dict[str, Any]:\n        ...\n检查点\n1. 上传函数是 async\n2. file.read 使用 await\n3. document 先创建为 processing\n4. chunks 保存成功后 document 更新为 completed\n5. 出错时 document 更新为 failed\n6. error_message 能写入\n7. chunk_count 更新正确\n8. char_count 更新正确\n9. 删除文档时删除 JSON 记录、chunks、原始文件\n参考实现：document_service.py\nfrom pathlib import Path\nfrom typing import Any\n\nfrom fastapi import UploadFile\n\nfrom src.app.document_store import (\n    create_document,\n    create_document_chunks,\n    delete_document,\n    delete_document_chunks,\n    get_document,\n    list_document_chunks,\n    list_documents,\n    update_document,\n)\nfrom src.app.exceptions import AppError\nfrom src.app.services.chunk_splitter import ChunkSplitter\nfrom src.app.services.document_loader import DocumentLoader\n\n\nclass DocumentService:\n    def __init__(self) -> None:\n        self.loader = DocumentLoader()\n        self.splitter = ChunkSplitter()\n\n    async def upload_document(self, file: UploadFile) -> dict[str, Any]:\n        filename = file.filename or \"\"\n\n        document: dict[str, Any] | None = None\n\n        try:\n            content_bytes = await file.read()\n\n            file_type = self.loader.validate_file(\n                filename=filename,\n                content=content_bytes,\n            )\n\n            text = self.loader.load_text(\n                filename=filename,\n                content=content_bytes,\n            )\n\n            saved_path = self.loader.save_upload_file(\n                filename=filename,\n                content=content_bytes,\n            )\n\n            document = create_document(\n                filename=filename,\n                file_type=file_type,\n                source_path=str(saved_path),\n                content=text,\n                status=\"processing\",\n            )\n\n            chunk_drafts = self.splitter.split(\n                text=text,\n                file_type=file_type,\n            )\n\n            if not chunk_drafts:\n                raise AppError(\n                    code=\"DOCUMENT_CHUNK_EMPTY\",\n                    message=\"文档没有生成有效 Chunk\",\n                    status_code=400,\n                )\n\n            chunk_records = create_document_chunks(\n                document_id=document[\"id\"],\n                chunks=chunk_drafts,\n            )\n\n            updated_document = update_document(\n                document[\"id\"],\n                {\n                    \"status\": \"completed\",\n                    \"chunk_count\": len(chunk_records),\n                    \"char_count\": len(text),\n                    \"error_message\": None,\n                },\n            )\n\n            return {\n                \"document_id\": updated_document[\"id\"],\n                \"filename\": updated_document[\"filename\"],\n                \"file_type\": updated_document[\"file_type\"],\n                \"status\": updated_document[\"status\"],\n                \"chunk_count\": updated_document[\"chunk_count\"],\n                \"char_count\": updated_document[\"char_count\"],\n                \"created_at\": updated_document[\"created_at\"],\n            }\n\n        except Exception as exc:\n            if document is not None:\n                update_document(\n                    document[\"id\"],\n                    {\n                        \"status\": \"failed\",\n                        \"error_message\": str(exc),\n                    },\n                )\n\n            raise\n\n    def list_documents(self) -> list[dict[str, Any]]:\n        documents = list_documents()\n\n        return [\n            self._without_content(document)\n            for document in documents\n        ]\n\n    def get_document_detail(self, document_id: str) -> dict[str, Any]:\n        document = get_document(document_id)\n\n        if document is None:\n            raise AppError(\n                code=\"DOCUMENT_NOT_FOUND\",\n                message=\"文档不存在\",\n                detail=f\"document_id={document_id}\",\n                status_code=404,\n            )\n\n        return document\n\n    def get_document_chunks(self, document_id: str) -> list[dict[str, Any]]:\n        document = get_document(document_id)\n\n        if document is None:\n            raise AppError(\n                code=\"DOCUMENT_NOT_FOUND\",\n                message=\"文档不存在\",\n                detail=f\"document_id={document_id}\",\n                status_code=404,\n            )\n\n        return list_document_chunks(document_id)\n\n    def delete_document(self, document_id: str) -> dict[str, Any]:\n        document = get_document(document_id)\n\n        if document is None:\n            raise AppError(\n                code=\"DOCUMENT_NOT_FOUND\",\n                message=\"文档不存在\",\n                detail=f\"document_id={document_id}\",\n                status_code=404,\n            )\n\n        source_path = document.get(\"source_path\")\n        if source_path:\n            path = Path(source_path)\n            if path.exists() and path.is_file():\n                path.unlink()\n\n        delete_document_chunks(document_id)\n        delete_document(document_id)\n\n        return {\n            \"success\": True,\n            \"message\": \"文档已删除\",\n        }\n\n    def _without_content(self, document: dict[str, Any]) -> dict[str, Any]:\n        return {\n            key: value\n            for key, value in document.items()\n            if key != \"content\"\n        }\n逐行解释\nfilename = file.filename or \"\"\nUploadFile.filename 可能为空。\n\n所以先兜底为空字符串，然后交给 loader 校验。\n\ncontent_bytes = await file.read()\n上传文件读取是异步操作。\n\n因为 FastAPI 上传文件来自 HTTP 请求流，使用 await 不会阻塞事件循环。\n\n为什么先 create_document 再切 chunk\n流程是：\n\n先创建 document\n再切 chunks\n而不是：\n\n先切 chunks\n再创建 document\n原因：\n\n真实系统中 document 是任务主记录\n即使后续解析失败，也应该知道这次上传发生过\nstatus/error_message 才有地方记录\n第五周当前 loader 校验失败前还没创建 document，这是可以接受的。\n\n后续如果要记录所有失败上传，可以更早创建 pending。\n\n为什么 status 是状态机\nprocessing -> completed\nprocessing -> failed\n真实系统里这里会变成异步任务：\n\n上传接口立即返回 pending\n后台 worker 解析\n完成后更新 completed\n失败后更新 failed\n第五周先同步处理，降低复杂度。\n\n_without_content\n列表接口不返回 content。\n\n防止文档多时响应过大。\n\n测试命令\npython -m compileall src/app\n常见错误\n1. NameError: list_documents\n你在类方法里导入的 store 函数和方法同名：\n\ndef list_documents(self):\n    documents = list_documents()\n这在 Python 中容易混淆，但可以工作，因为方法作用域里找全局函数。\n\n更清晰写法是给 store 起别名：\n\nfrom src.app import document_store\n\ndocuments = document_store.list_documents()\n如果你觉得困惑，可以改成别名版。\n\n2. 删除文件失败\n可能是 source_path 是相对路径，或者文件不存在。\n\n本实现做了：\n\nif path.exists() and path.is_file():\n    path.unlink()\n不存在不报错。\n\n复盘问题\n1. 为什么 DocumentService 是编排层？\n2. 为什么 loader / splitter / store 要拆开？\n3. 为什么上传文档像一个状态机？\n4. 为什么真实系统会改成异步任务？\n可面试表达\nDocumentService 是文档处理的应用服务层，负责编排 UploadFile 读取、DocumentLoader 校验和文本解析、ChunkSplitter 切分、document_store 持久化，以及 document status 更新。它本身不写正则切分逻辑，也不直接处理 JSON 细节，保持了业务流程和底层实现的解耦。\nDay 5：接入 Documents API\n目标\n今天在 src/app/api/routes.py 接入：\n\nPOST /documents/upload\nGET /documents\nGET /documents/{document_id}\nGET /documents/{document_id}/chunks\nDELETE /documents/{document_id}\n核心思想\n路由层只做：\n\nHTTP 参数接收\n调用 service\nresponse_model 包装\n不要把上传、解析、切分逻辑写到 routes.py。\n\n为什么\n当前 routes.py 已经集中管理所有接口。\n\n第五周为了低重构，继续放这里。\n\n但要保持职责边界：\n\nroutes.py：HTTP 层\ndocument_service.py：业务编排\ndocument_loader.py：文件处理\nchunk_splitter.py：文本切分\ndocument_store.py：JSON 存储\n依赖补充：python-multipart\nFastAPI 处理 multipart/form-data 上传需要 python-multipart。\n\n你当前 pyproject.toml 里没有这个依赖。\n\n添加：\n\nuv add python-multipart\n或手动在 pyproject.toml dependencies 里加入：\n\n\"python-multipart>=0.0.20\",\n如果缺少它，FastAPI 通常会报：\n\nForm data requires \"python-multipart\" to be installed.\n怎么做\n修改 import\nroutes.py 增加：\n\nfrom fastapi import APIRouter, File, HTTPException, UploadFile\nSchema import 增加：\n\nDocumentChunkItem,\nDocumentChunkListResponse,\nDocumentDetailResponse,\nDocumentItem,\nDocumentListResponse,\nUploadDocumentResponse,\nDeleteDocumentResponse,\nService import 增加：\n\nfrom src.app.services.document_service import DocumentService\n你自己先写\n先只写上传接口。\n\n跑通后再写 list/detail/chunks/delete。\n\n代码骨架\n@router.post(\"/documents/upload\", response_model=UploadDocumentResponse)\nasync def upload_document_api(\n    file: UploadFile = File(...),\n) -> UploadDocumentResponse:\n    ...\n\n\n@router.get(\"/documents\", response_model=DocumentListResponse)\ndef list_documents_api() -> DocumentListResponse:\n    ...\n\n\n@router.get(\"/documents/{document_id}\", response_model=DocumentDetailResponse)\ndef get_document_api(document_id: str) -> DocumentDetailResponse:\n    ...\n\n\n@router.get(\"/documents/{document_id}/chunks\", response_model=DocumentChunkListResponse)\ndef list_document_chunks_api(document_id: str) -> DocumentChunkListResponse:\n    ...\n\n\n@router.delete(\"/documents/{document_id}\", response_model=DeleteDocumentResponse)\ndef delete_document_api(document_id: str) -> DeleteDocumentResponse:\n    ...\n检查点\n1. 上传接口使用 async\n2. 上传参数使用 File(...)\n3. UploadFile 从 fastapi 导入\n4. GET 接口不需要 async\n5. routes.py 不写 splitter 逻辑\n6. response_model 正确\n7. 返回值转换成 Pydantic Response\n参考实现：routes.py 新增部分\nfrom fastapi import APIRouter, File, HTTPException, UploadFile\n在 schemas import 中补充：\n\n    DocumentChunkItem,\n    DocumentChunkListResponse,\n    DocumentDetailResponse,\n    DocumentItem,\n    DocumentListResponse,\n    UploadDocumentResponse,\n    DeleteDocumentResponse,\n新增 service import：\n\nfrom src.app.services.document_service import DocumentService\n追加路由：\n\n@router.post(\"/documents/upload\", response_model=UploadDocumentResponse)\nasync def upload_document_api(\n    file: UploadFile = File(...),\n) -> UploadDocumentResponse:\n    result = await DocumentService().upload_document(file)\n    return UploadDocumentResponse(**result)\n\n\n@router.get(\"/documents\", response_model=DocumentListResponse)\ndef list_documents_api() -> DocumentListResponse:\n    documents = DocumentService().list_documents()\n    return DocumentListResponse(\n        items=[DocumentItem(**item) for item in documents]\n    )\n\n\n@router.get(\"/documents/{document_id}\", response_model=DocumentDetailResponse)\ndef get_document_api(document_id: str) -> DocumentDetailResponse:\n    document = DocumentService().get_document_detail(document_id)\n    return DocumentDetailResponse(**document)\n\n\n@router.get(\n    \"/documents/{document_id}/chunks\",\n    response_model=DocumentChunkListResponse,\n)\ndef list_document_chunks_api(document_id: str) -> DocumentChunkListResponse:\n    chunks = DocumentService().get_document_chunks(document_id)\n    return DocumentChunkListResponse(\n        items=[DocumentChunkItem(**item) for item in chunks]\n    )\n\n\n@router.delete(\n    \"/documents/{document_id}\",\n    response_model=DeleteDocumentResponse,\n)\ndef delete_document_api(document_id: str) -> DeleteDocumentResponse:\n    result = DocumentService().delete_document(document_id)\n    return DeleteDocumentResponse(**result)\n逐行解释\nFile(...)\nfile: UploadFile = File(...)\n告诉 FastAPI：\n\n这个参数来自 multipart/form-data 文件字段\n不是 JSON body。\n\nUploadFile\nFastAPI 的文件上传对象。\n\n它包含：\n\nfilename\ncontent_type\nfile\nread()\n为什么上传用 POST\n上传文档会创建资源：\n\n创建 document\n创建 chunks\n保存原始文件\n写 JSON\n所以用 POST。\n\n为什么是 multipart/form-data\n因为上传的是文件，不是普通 JSON。\n\n文件上传需要携带二进制内容。\n\n为什么查询用 GET\n查询不改变系统状态。\n\nGET /documents\nGET /documents/{document_id}\nGET /documents/{document_id}/chunks\n都是读取资源。\n\n为什么 chunks 按 document_id 查询\nChunk 是 Document 的子资源。\n\n路径表达资源关系：\n\n/documents/{document_id}/chunks\n意思是：\n\n查询某个文档下的 chunks\n为什么删除用 DELETE\n删除 document 资源。\n\n第五周删除只处理：\n\ndocument JSON 记录\ndocument_chunks JSON 记录\n本地原始文件\n因为还没有向量库。\n\n测试命令\n先启动：\n\npython -m src.app.main\n如果使用 uv：\n\nuv run python -m src.app.main\n准备测试文件：\n\ncat > /tmp/fastapi-guide.md <<'EOF'\n# FastAPI 异常处理\n\nFastAPI 可以通过 exception_handler 注册全局异常处理器。\n\n## AppError\n\nAppError 是我们自定义的业务异常基类。\n\n## JSONResponse\n\nJSONResponse 可以自定义异常返回的 JSON 结构。\n\n## RequestValidationError\n\nRequestValidationError 用来处理请求参数验证失败。\nEOF\n上传：\n\ncurl -X POST http://127.0.0.1:8000/documents/upload \\\n  -F \"file=@/tmp/fastapi-guide.md\"\n查询列表：\n\ncurl http://127.0.0.1:8000/documents\n查询详情：\n\ncurl http://127.0.0.1:8000/documents/{document_id}\n查询 chunks：\n\ncurl http://127.0.0.1:8000/documents/{document_id}/chunks\n删除：\n\ncurl -X DELETE http://127.0.0.1:8000/documents/{document_id}\ncurl 解释\n-X\n指定 HTTP 方法。\n\n例如：\n\n-X POST\n-X DELETE\n-F\n发送表单字段。\n\n上传文件时：\n\n-F \"file=@/tmp/fastapi-guide.md\"\n表示：\n\n字段名叫 file\n字段内容来自 /tmp/fastapi-guide.md 这个文件\nmultipart/form-data\n浏览器上传文件时常用的请求格式。\n\n它可以同时传：\n\n文本字段\n文件字段\n二进制内容\n为什么不用 JSON\n不要这样：\n\n-H \"Content-Type: application/json\"\n因为文件上传不是 JSON body。\n\n常见错误\n1. python-multipart 缺失\n执行：\n\nuv add python-multipart\n2. 422 Validation Error\n检查 curl 字段名是否是：\n\nfile\n必须和接口参数一致：\n\nfile: UploadFile = File(...)\n3. 404 Document Not Found\n替换 {document_id} 时不要带花括号。\n\n错误：\n\ncurl http://127.0.0.1:8000/documents/{document_id}\n正确：\n\ncurl http://127.0.0.1:8000/documents/真实uuid\n复盘问题\n1. 为什么上传接口用 POST？\n2. 为什么 UploadFile 要配合 File(...)？\n3. 为什么路由层不写切分逻辑？\n4. 为什么 chunks 是 document 的子资源？\n可面试表达\nDocuments API 采用资源化设计：POST /documents/upload 创建文档资源，GET /documents 查询文档集合，GET /documents/{id} 查询文档详情，GET /documents/{id}/chunks 查询子资源 chunks，DELETE /documents/{id} 删除文档。路由层只负责 HTTP 参数接收和 response_model 包装，真正的上传、解析、切分、保存由 DocumentService 编排。\nDay 6：完整测试、错误处理和排查\n目标\n今天完成：\n\n1. md 上传验收\n2. txt 上传验收\n3. 非法文件类型测试\n4. 空文件测试\n5. 超大文件测试\n6. chunks 结果检查\n7. JSON 文件检查\n8. uploads 文件检查\n核心思想\n第五周验收不是“接口返回 200”就结束。\n\n真正要检查：\n\ndocument 是否正确保存\nchunk 是否合理\nheading 是否保留\nstatus 是否正确\nerror_message 是否正确\nsource_path 是否可追踪\n为什么\n文档处理是 RAG 的前置数据工程。\n\n后面 embedding / RAG 出问题，很多时候不是模型问题，而是：\n\n原始文本没解析对\nchunk 切错\nchunk 太短\nheading 丢失\nmetadata 缺失\nstatus 不准\n完整测试 1：上传 Markdown\ncat > /tmp/fastapi-guide.md <<'EOF'\n# FastAPI 异常处理\n\nFastAPI 可以通过 exception_handler 注册全局异常处理器。\n\n## AppError\n\nAppError 是我们自定义的业务异常基类。\n\n## JSONResponse\n\nJSONResponse 可以自定义异常返回的 JSON 结构。\n\n## RequestValidationError\n\nRequestValidationError 用来处理请求参数验证失败。\nEOF\n上传：\n\ncurl -X POST http://127.0.0.1:8000/documents/upload \\\n  -F \"file=@/tmp/fastapi-guide.md\"\n期望响应：\n\n{\n  \"document_id\": \"uuid\",\n  \"filename\": \"fastapi-guide.md\",\n  \"file_type\": \"md\",\n  \"status\": \"completed\",\n  \"chunk_count\": 4,\n  \"char_count\": 160,\n  \"created_at\": \"...\"\n}\nchar_count 不一定完全等于示例值，以实际文本长度为准。\n\n完整测试 2：查询文档列表\ncurl http://127.0.0.1:8000/documents\n期望：\n\n{\n  \"items\": [\n    {\n      \"id\": \"uuid\",\n      \"filename\": \"fastapi-guide.md\",\n      \"file_type\": \"md\",\n      \"source_path\": \"src/app/data/uploads/uuid_fastapi-guide.md\",\n      \"status\": \"completed\",\n      \"chunk_count\": 4,\n      \"char_count\": 160,\n      \"error_message\": null,\n      \"created_at\": \"...\",\n      \"updated_at\": \"...\"\n    }\n  ]\n}\n列表接口不应该返回 content。\n\n完整测试 3：查询文档详情\ncurl http://127.0.0.1:8000/documents/{document_id}\n期望详情包含：\n\n{\n  \"id\": \"uuid\",\n  \"filename\": \"fastapi-guide.md\",\n  \"file_type\": \"md\",\n  \"source_path\": \"src/app/data/uploads/uuid_fastapi-guide.md\",\n  \"status\": \"completed\",\n  \"content\": \"# FastAPI 异常处理\\n\\n...\",\n  \"chunk_count\": 4,\n  \"char_count\": 160,\n  \"error_message\": null,\n  \"created_at\": \"...\",\n  \"updated_at\": \"...\"\n}\n完整测试 4：查询 chunks\ncurl http://127.0.0.1:8000/documents/{document_id}/chunks\n期望：\n\nchunk 0:\nheading = FastAPI 异常处理\ncontent 包含 FastAPI exception_handler\n\nchunk 1:\nheading = AppError\ncontent 包含 AppError 是业务异常基类\n\nchunk 2:\nheading = JSONResponse\ncontent 包含 JSONResponse 可以自定义 JSON 结构\n\nchunk 3:\nheading = RequestValidationError\ncontent 包含请求参数验证失败\n完整测试 5：上传 txt\ncat > /tmp/simple-note.txt <<'EOF'\n这是一个普通文本文件。\n\n第一段介绍 FastAPI。\n\n第二段介绍异常处理。\n\n第三段介绍 JSONResponse。\nEOF\n\ncurl -X POST http://127.0.0.1:8000/documents/upload \\\n  -F \"file=@/tmp/simple-note.txt\"\n期望：\n\nfile_type = txt\nstatus = completed\nchunk_count >= 1\nheading = null\n完整测试 6：非法文件类型\ncat > /tmp/bad.pdf <<'EOF'\nfake pdf\nEOF\n\ncurl -X POST http://127.0.0.1:8000/documents/upload \\\n  -F \"file=@/tmp/bad.pdf\"\n期望：\n\n{\n  \"code\": \"DOCUMENT_UNSUPPORTED_FILE_TYPE\",\n  \"message\": \"暂只支持 .md 和 .txt 文件\",\n  \"detail\": \"filename=bad.pdf\"\n}\n你的统一异常处理会捕获 AppError 并返回结构化 JSON。当前项目已经注册了 AppError 处理器。\n\n完整测试 7：空文件\ntouch /tmp/empty.md\n\ncurl -X POST http://127.0.0.1:8000/documents/upload \\\n  -F \"file=@/tmp/empty.md\"\n期望：\n\n{\n  \"code\": \"DOCUMENT_EMPTY_FILE\",\n  \"message\": \"上传文件不能为空\"\n}\n完整测试 8：超大文件\npython - <<'PY'\nfrom pathlib import Path\nPath(\"/tmp/large.md\").write_text(\"a\" * (6 * 1024 * 1024), encoding=\"utf-8\")\nPY\n\ncurl -X POST http://127.0.0.1:8000/documents/upload \\\n  -F \"file=@/tmp/large.md\"\n期望：\n\n{\n  \"code\": \"DOCUMENT_FILE_TOO_LARGE\",\n  \"message\": \"上传文件过大\"\n}\n排查清单\n如果 chunks 不符合预期，按顺序检查：\n\n1. document 是否写入\n检查：\n\ncat src/app/data/documents.json\n如果你的 DATA_DIR 后续改成根目录，则检查：\n\ncat data/documents.json\n2. chunk 是否写入\ncat src/app/data/document_chunks.json\n3. source_path 是否正确\n检查 document：\n\n\"source_path\": \"src/app/data/uploads/uuid_fastapi-guide.md\"\n4. 上传文件是否保存\nls -la src/app/data/uploads\n5. file_type 是否识别为 md\n检查：\n\n\"file_type\": \"md\"\n不是：\n\n\"file_type\": \".md\"\n6. Markdown 标题正则是否正确\n必须有：\n\nre.MULTILINE\n7. splitter 是否保留 heading\n检查 chunk：\n\n\"heading\": \"JSONResponse\"\n8. chunk_index 是否连续\n应为：\n\n0, 1, 2, 3\n9. char_count 是否正确\n可以临时验证：\n\nlen(chunk[\"content\"])\n10. estimated_tokens 是否合理\n中文短句 token 估算应该接近字符数。\n\n11. document.status 是否为 completed\n\"status\": \"completed\"\n12. error_message 是否为 null\n\"error_message\": null\n常见错误\n1. Form data requires \"python-multipart\"\n安装：\n\nuv add python-multipart\n2. 上传成功但 chunk_count 是 0\n检查 splitter 是否返回空数组。\n\n可能原因：\n\nMarkdown 只有标题没有正文\nMIN_CHUNK_CHARS 过滤太严格\nsplit_markdown 没有 fallback\n3. Markdown heading 为 null\n说明走了 txt/plain 逻辑。\n\n检查：\n\nfile_type 是否是 md\n4. DOCUMENT_NOT_FOUND\ndocument_id 写错。\n\n先查询列表：\n\ncurl http://127.0.0.1:8000/documents\n复制真实 id。\n\n复盘问题\n1. 为什么上传成功要检查 JSON 文件？\n2. 为什么 chunk_count 是核心验收指标？\n3. 为什么 heading 对 RAG sources 很重要？\n4. 为什么错误也要结构化返回？\n可面试表达\n我不仅实现了文档上传接口，还设计了可调试的验收链路。上传后可以通过 documents.json 查看文档主记录，通过 document_chunks.json 查看切分结果，通过 uploads 目录定位原始文件。每个 chunk 都有 heading、chunk_index、char_count、estimated_tokens 和 metadata，便于后续 embedding、检索、溯源和问题排查。\nDay 7：README、dev-log、复盘和面试表达\n目标\n今天完成：\n\n1. README 增加 Week 5 说明\n2. docs/dev-log.md 增加第五周日志模板\n3. 整理最终验收标准\n4. 准备面试表达\n核心思想\n项目不是“写完代码就结束”。\n\nAI 转型项目要能：\n\n跑得通\n讲得清\n查得到\n可复盘\n可展示\nREADME 更新内容\n在 README.md 增加：\n\n## Week 5：文档上传 + Chunk 切分\n\n### 本周目标\n\n实现 AI 知识库的最小文档处理层：\n\n```text\n上传文档\n  ↓\n保存原始文件\n  ↓\n解析文本\n  ↓\n创建 Document\n  ↓\n切分 Chunk\n  ↓\n保存 DocumentChunk\n  ↓\n提供查询和调试接口\n为什么需要 Document 层\nDocument 是知识库的一级资源，用来记录原始文件、文件类型、保存路径、解析文本、状态、错误信息和统计信息。\n\n为什么第五周只做 md/txt\nmd/txt 本质是纯文本，可以聚焦 RAG 前置管线：\n\ndocument -> text -> chunk\n为什么不做 PDF\nPDF 会引入版面分析、坐标、字体、OCR、阅读顺序等复杂问题，容易偏离本周主线。\n\n为什么需要 Chunk\nChunk 是后续 Embedding 和 RAG 检索的最小语义单元。Chunk 切分质量会直接影响后续召回质量和回答可信度。\n\n新增文件\nsrc/app/document_store.py\nsrc/app/services/document_loader.py\nsrc/app/services/chunk_splitter.py\nsrc/app/services/document_service.py\n修改文件\nsrc/app/paths.py\nsrc/app/schemas.py\nsrc/app/api/routes.py\nsrc/app/config.py\n新增接口\nPOST /documents/upload\nGET /documents\nGET /documents/{document_id}\nGET /documents/{document_id}/chunks\nDELETE /documents/{document_id}\n测试命令\ncat > /tmp/fastapi-guide.md <<'EOF'\n# FastAPI 异常处理\n\nFastAPI 可以通过 exception_handler 注册全局异常处理器。\n\n## AppError\n\nAppError 是我们自定义的业务异常基类。\n\n## JSONResponse\n\nJSONResponse 可以自定义异常返回的 JSON 结构。\n\n## RequestValidationError\n\nRequestValidationError 用来处理请求参数验证失败。\nEOF\n\ncurl -X POST http://127.0.0.1:8000/documents/upload \\\n  -F \"file=@/tmp/fastapi-guide.md\"\n\ncurl http://127.0.0.1:8000/documents\ncurl http://127.0.0.1:8000/documents/{document_id}\ncurl http://127.0.0.1:8000/documents/{document_id}/chunks\n当前限制\n只支持 .md / .txt\n只支持 UTF-8\n同步处理上传\nJSON 文件存储\n不支持 PDF\n不支持 OCR\n不做 Embedding\n不做 RAG\n下一周计划\nWeek 6：Embedding + pgvector\n\nchunk\n  ↓\nembedding model\n  ↓\nvector\n  ↓\npgvector\n\n---\n\n## dev-log 模板\n\n如果当前没有 `docs/dev-log.md`，新建。\n\n追加：\n\n```markdown\n## Week 5 Day X\n\n日期：\n本日目标：\n核心思想：\n完成内容：\n遇到问题：\n如何解决：\n明天计划：\n可面试表达：\n建议每天一条。\n\n你的总体计划也强调每周要有 dev-log、README、可演示功能、可面试讲点。\n\n最终验收标准\n第五周结束后，必须能做到：\n\n1. python -m src.app.main 能启动\n2. POST /documents/upload 能上传 md\n3. POST /documents/upload 能上传 txt\n4. GET /documents 能查询列表\n5. GET /documents/{document_id} 能查询详情\n6. GET /documents/{document_id}/chunks 能查询 chunks\n7. data documents JSON 中能看到 document\n8. data document_chunks JSON 中能看到 chunks\n9. uploads 中能看到原始文件\n10. Markdown 标题能变成 chunk.heading\n11. chunk_index 连续\n12. char_count 正确\n13. estimated_tokens 合理\n14. document.status 正常是 completed\n15. 非法文件类型会报错\n16. 空文件会报错\n17. 超大文件会报错\n18. README 有 Week 5 说明\n19. dev-log 有第五周记录\n20. 能说明这是 RAG 前置工程，而不是 RAG 本身\n复盘问题\n1. Document 和文件有什么区别？\n2. Chunk 和普通字符串切片有什么区别？\n3. 为什么第五周不做 Embedding？\n4. 为什么第五周不做 RAG？\n5. 为什么 PDF 要单独拆专题？\n6. 为什么 JSON Store 只是阶段性方案？\n可面试表达\n第五周我实现了知识库的最小文档处理层。系统支持上传 md/txt 文档，会校验类型和大小，保存原始文件，解析 UTF-8 文本，创建 Document 主记录，并通过 ChunkSplitter 按 Markdown 标题、段落和长度兜底策略生成 DocumentChunk。每个 chunk 都保留 heading、chunk_index、char_count、estimated_tokens 和 metadata。这个阶段没有做 Embedding 和 RAG，因为它的目标是先把 RAG 的前置数据管线打稳，为下一周的向量化和 pgvector 做准备。\n常见报错排查\n1. Form data requires \"python-multipart\"\n原因：\n\nFastAPI 使用 File(...) 处理 multipart/form-data 时需要 python-multipart。\n解决：\n\nuv add python-multipart\n2. 422 Unprocessable Entity\n常见原因：\n\ncurl 字段名不是 file\n用了 JSON 上传\n没有加 -F\n正确：\n\ncurl -X POST http://127.0.0.1:8000/documents/upload \\\n  -F \"file=@/tmp/fastapi-guide.md\"\n3. DOCUMENT_UNSUPPORTED_FILE_TYPE\n原因：\n\n后缀不是 .md / .txt\n第五周不支持 PDF。\n\n4. DOCUMENT_DECODE_ERROR\n原因：\n\n不是 UTF-8 编码\n解决：\n\niconv -f gbk -t utf-8 old.txt > new.txt\n5. chunk_count = 0\n排查：\n\n1. text 是否为空\n2. Markdown 标题下是否有正文\n3. splitter 是否过滤了短 chunk\n4. file_type 是否是 md/txt\n5. split_markdown 是否 fallback 到 plain text\n6. heading = null\n如果上传的是 txt，这是正常的。\n\n如果上传的是 md，则检查：\n\n标题是否是 \"# 标题\"\n不是 \"#标题\"\n7. 找不到 JSON 文件\n当前项目 DATA_DIR 是：\n\nsrc/app/data\n不是根目录 data。\n\n检查：\n\nls src/app/data\n本周必须掌握的概念\n1. UploadFile\nFastAPI 的文件上传对象。\n\n核心点：\n\n它来自 multipart/form-data\nawait file.read() 返回 bytes\n需要 decode 成文本\n2. Document\n知识库一级资源。\n\n不是临时文件。\n\n它负责：\n\n来源\n状态\n原文\n统计\n错误\n溯源\n3. Chunk\nRAG 检索最小语义单元。\n\n它影响：\n\nembedding 表达\n检索召回\n回答质量\nsources 可解释性\n4. Loader\n外部文件进入系统的安全入口。\n\n负责：\n\n校验\n保存\n解码\n读取文本\n5. Splitter\n文本切分策略模块。\n\n不要让它：\n\n访问数据库\n调用 LLM\n处理 UploadFile\n6. metadata\n不是附加小字段。\n\n它是后续：\n\n过滤\n溯源\n调试\n引用\n评测\n的基础。\n\n7. status\n不是展示字段。\n\n它是后续异步处理、失败恢复、任务追踪的基础。\n\n第五周完成后的面试表达\n30 秒版本\n我在 AI Chat Backend 的基础上增加了知识库文档处理层。系统支持上传 md/txt 文档，校验文件类型和大小，保存原始文件，解析 UTF-8 文本，创建 Document 记录，并按 Markdown 标题、段落和长度兜底策略切分成 chunks。每个 chunk 保留 heading、chunk_index、char_count、estimated_tokens 和 metadata，为后续 Embedding、pgvector 和 RAG 检索做准备。\n1 分钟版本\n第五周我没有直接做 RAG，而是先实现 RAG 前置的数据管线。因为 RAG 的效果很大程度取决于文档解析和 chunk 切分质量。我设计了 Document、DocumentChunk、DocumentLoader、ChunkSplitter、DocumentService 和 document_store。Document 管理文件来源、状态、原文和错误；Chunk 是后续 embedding 和检索的最小语义单元；Loader 负责上传文件校验和文本读取；Splitter 负责按文档结构切分；Service 负责编排整个上传、解析、切分、保存流程。当前阶段继续用 JSON 存储，下一周再进入 Embedding 和 pgvector。\n3 分钟版本结构\n1. 为什么不能直接做 RAG\n2. 为什么要先有 Document / Chunk 层\n3. 上传文件如何校验和保存\n4. Markdown 如何按标题切分\n5. Chunk 为什么影响 embedding 和检索效果\n6. 为什么 status/error_message/metadata 很重要\n7. 为什么第五周不做 PDF、Embedding、RAG\n8. 下一步如何接 pgvector\n最后提醒：第五周不要跑偏\n第五周只做：\n\n.md / .txt 上传\n文本读取\nDocument 保存\nChunk 切分\nChunk 查询\n调试预览\nREADME / dev-log\n不要做：\n\nEmbedding\npgvector\nPostgreSQL\nSQLAlchemy\nRAG\nQuery Rewrite\nAgent\nMCP\nPDF\nOCR\nWord / Excel / PPT\n复杂前端页面\n第五周的核心验收指标只有一个：\n\n上传一个文档后，能稳定得到结构合理、可查询、可调试的 chunks。",
    "status": "completed",
    "chunk_count": 67,
    "char_count": 58833,
    "error_message": null,
    "created_at": "2026-05-14T12:29:40.689709",
    "updated_at": "2026-05-14T12:29:40.698183"
  }
]