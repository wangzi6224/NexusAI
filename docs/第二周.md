# 第二周教程：练习驱动版

你说得对。上一版教程的问题是：**我把代码都写好了，你只是在复制**。这对学习 Python 后端、AI 工程没有太大帮助。

这版改成：

```text
先讲目标
再拆任务
再给你自己写的提示
再给检查方式
最后才给参考答案
```

你应该按这个节奏执行：

```text
1. 先不要看参考答案
2. 根据任务要求自己写
3. 跑命令验证
4. 报错就看提示
5. 实在写不出来再看参考答案
```

---

# 第二周总目标

基于你现在的项目，把第一周的“能调用 Ollama”升级成更真实的 AI 后端服务。

你当前 `OllamaProvider.chat()` 已经能非流式调用 Ollama，而且 payload 里明确写了 `"stream": False`。

第二周要完成：

```text
1. 配置系统：从零散 os.getenv 升级为 Settings 对象
2. 异常系统：把 RuntimeError 升级为可分类的业务异常
3. 日志增强：记录请求、模型、耗时、异常
4. 流式输出：新增 /chat/stream
5. 错误治理：修复 /models 调 Ollama 失败时错误不可控的问题
```

---

# 学习方式约定

每个任务我会分成 5 块：

```text
目标：你要做成什么
为什么：这个东西在工程里解决什么问题
你先自己写：不给完整代码，只给方向
检查点：怎么知道你写对了
参考答案：卡住时再看
```

你要优先看：

```text
目标
为什么
你先自己写
检查点
```

不要一上来就看参考答案。

---

# Day 1：配置系统 Settings

## 1.1 目标

把现在这种配置读取方式：

```python
get_ollama_base_url()
get_ollama_model()
get_ollama_timeout()
get_log_level()
```

升级成：

```python
settings.ollama_base_url
settings.ollama_model
settings.ollama_timeout
settings.app_log_level
```

但是注意：**不要一下子全项目大改**。

你这周的目标是：

```text
新增 Settings 对象
保留旧函数作为兼容层
```

也就是说，旧代码还能继续这样用：

```python
get_ollama_base_url()
```

但内部实际已经改成：

```python
get_settings().ollama_base_url
```

---

## 1.2 为什么要这么做？

你现在配置少，所以函数式读取还能接受。

但后面你会增加：

```text
DATABASE_URL
EMBEDDING_MODEL
TOP_K
SCORE_THRESHOLD
CHUNK_SIZE
CHUNK_OVERLAP
MAX_AGENT_STEPS
MCP_BACKEND_URL
```

如果还到处写：

```python
os.getenv("xxx")
```

项目会越来越乱。

统一 Settings 的好处是：

```text
1. 所有配置集中在一个类里
2. 配置有默认值
3. 配置有类型校验
4. 配置名称清晰
5. 后续测试和部署更容易
```

---

## 1.3 先安装依赖

你自己执行：

```bash
uv add pydantic-settings
```

然后检查：

```bash
grep "pydantic-settings" pyproject.toml
```

如果能看到依赖，说明安装成功。

---

## 1.4 你先自己写

打开：

```text
src/app/config.py
```

你要自己完成这些事情：

```text
1. 引入 BaseSettings
2. 定义 Settings 类
3. 给 Settings 加这些字段：
   - app_env
   - app_log_level
   - ollama_base_url
   - ollama_model
   - ollama_timeout
   - ollama_keep_alive
   - chat_history_path
4. 定义 get_settings()
5. 用 @lru_cache 缓存 get_settings()
6. 保留原来的 get_ollama_base_url 等函数
7. 让旧函数内部读取 get_settings()
```

先不要看参考答案。

你可以根据这个骨架自己补：

```python
from functools import lru_cache

from pydantic import Field
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    # 你自己补字段
    pass


@lru_cache
def get_settings() -> Settings:
    # 你自己补返回
    pass


def get_ollama_base_url() -> str:
    # 你自己补
    pass


def get_ollama_model() -> str:
    # 你自己补
    pass


def get_ollama_timeout() -> int:
    # 你自己补
    pass


def get_ollama_keep_alive() -> str:
    # 你自己补
    pass


def get_log_level() -> str:
    # 你自己补
    pass


def get_chat_history_path() -> str:
    # 你自己补
    pass
```

---

## 1.5 检查点

你写完后，执行：

```bash
python -c "from src.app.config import get_settings; print(get_settings())"
```

如果正常，会打印 Settings 对象。

再执行：

```bash
python -c "from src.app.config import get_ollama_base_url; print(get_ollama_base_url())"
```

应该输出：

```text
http://localhost:11434
```

或者你 `.env` 中配置的地址。

---

## 1.6 你必须理解的点

### `BaseSettings`

它不是处理用户请求的。

它处理的是：

```text
.env
环境变量
系统配置
```

### `Field(alias="OLLAMA_MODEL")`

意思是：

```text
Python 里叫 ollama_model
.env 里叫 OLLAMA_MODEL
```

### `@lru_cache`

意思是：

```text
Settings 只创建一次
后面反复复用
```

---

## 1.7 参考答案

卡住再看：

```python
from functools import lru_cache

from pydantic import Field
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    app_env: str = Field(default="development", alias="APP_ENV")
    app_log_level: str = Field(default="INFO", alias="APP_LOG_LEVEL")

    ollama_base_url: str = Field(
        default="http://localhost:11434",
        alias="OLLAMA_BASE_URL",
    )
    ollama_model: str = Field(
        default="qwen2.5:7b",
        alias="OLLAMA_MODEL",
    )
    ollama_timeout: int = Field(
        default=120,
        alias="OLLAMA_TIMEOUT",
    )
    ollama_keep_alive: str = Field(
        default="5m",
        alias="OLLAMA_KEEP_ALIVE",
    )

    chat_history_path: str = Field(
        default="chat_history.json",
        alias="CHAT_HISTORY_PATH",
    )

    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
    )


@lru_cache
def get_settings() -> Settings:
    return Settings()


def get_ollama_base_url() -> str:
    return get_settings().ollama_base_url.rstrip("/")


def get_ollama_model() -> str:
    return get_settings().ollama_model


def get_ollama_timeout() -> int:
    return get_settings().ollama_timeout


def get_ollama_keep_alive() -> str:
    return get_settings().ollama_keep_alive


def get_log_level() -> str:
    return get_settings().app_log_level


def get_chat_history_path() -> str:
    return get_settings().chat_history_path
```

---

# Day 2：统一异常系统

## 2.1 目标

把现在这种异常：

```python
raise RuntimeError("请求 Ollama 失败")
```

升级成：

```python
raise LLMProviderError(
    message="请求 Ollama 失败",
    detail=str(exc),
)
```

最终接口返回：

```json
{
  "error": {
    "code": "LLM_PROVIDER_ERROR",
    "message": "请求 Ollama 失败",
    "detail": "..."
  }
}
```

---

## 2.2 为什么要这么做？

`RuntimeError` 对人类开发者有意义，但对接口不够好。

因为前端需要知道：

```text
这个错误属于哪一类？
应该展示什么提示？
是否可以重试？
是否是用户参数错误？
是否是模型服务错误？
```

所以你要设计自己的异常类型。

---

## 2.3 你先自己写

新增文件：

```text
src/app/exceptions.py
```

你要自己完成：

```text
1. 定义 AppError
2. AppError 继承 Exception
3. AppError 初始化参数包括：
   - code
   - message
   - status_code
   - detail
4. 定义 LLMProviderError
5. LLMProviderError 继承 AppError
6. LLMProviderError 默认 code 为 LLM_PROVIDER_ERROR
7. 定义 ConfigError
8. ConfigError 默认 code 为 CONFIG_ERROR
```

先用这个骨架：

```python
class AppError(Exception):
    def __init__(
        self,
        code: str,
        message: str,
        status_code: int = 500,
        detail: str | None = None,
    ) -> None:
        # 你自己补
        pass


class LLMProviderError(AppError):
    def __init__(
        self,
        message: str = "模型服务调用失败",
        detail: str | None = None,
        status_code: int = 502,
    ) -> None:
        # 你自己补
        pass


class ConfigError(AppError):
    def __init__(
        self,
        message: str = "应用配置错误",
        detail: str | None = None,
        status_code: int = 500,
    ) -> None:
        # 你自己补
        pass
```

---

## 2.4 检查点

写完后，执行：

```bash
python -c "from src.app.exceptions import LLMProviderError; e = LLMProviderError(detail='test'); print(e.code, e.message, e.status_code, e.detail)"
```

应该输出类似：

```text
LLM_PROVIDER_ERROR 模型服务调用失败 502 test
```

---

## 2.5 你必须理解的点

### 为什么 `super().__init__(message)`？

因为 Python 原生异常对象本身也需要保存错误文本。

你写：

```python
super().__init__(message)
```

之后，下面这样才有意义：

```python
str(exc)
```

### 为什么要有 `status_code`？

因为不同错误应该对应不同 HTTP 状态：

```text
参数错误：400 / 422
模型服务错误：502
内部错误：500
未授权：401
权限不足：403
```

---

## 2.6 参考答案

```python
class AppError(Exception):
    def __init__(
        self,
        code: str,
        message: str,
        status_code: int = 500,
        detail: str | None = None,
    ) -> None:
        super().__init__(message)
        self.code = code
        self.message = message
        self.status_code = status_code
        self.detail = detail


class LLMProviderError(AppError):
    def __init__(
        self,
        message: str = "模型服务调用失败",
        detail: str | None = None,
        status_code: int = 502,
    ) -> None:
        super().__init__(
            code="LLM_PROVIDER_ERROR",
            message=message,
            status_code=status_code,
            detail=detail,
        )


class ConfigError(AppError):
    def __init__(
        self,
        message: str = "应用配置错误",
        detail: str | None = None,
        status_code: int = 500,
    ) -> None:
        super().__init__(
            code="CONFIG_ERROR",
            message=message,
            status_code=status_code,
            detail=detail,
        )
```

---

# Day 3：升级全局异常处理

## 3.1 目标

你现在已经有：

```text
src/app/api/exception_handlers.py
```

它现在能捕获未处理异常，但返回结构太简单。

你要把它升级成三层异常处理：

```text
1. AppError：你自己主动抛的业务异常
2. RequestValidationError：请求参数错误
3. Exception：兜底未知异常
```

---

## 3.2 为什么要这么做？

因为后端错误大体分三类：

```text
可预期业务错误
请求参数错误
系统未知错误
```

它们不能都返回：

```json
{"detail": "服务器内部错误"}
```

否则前端不知道怎么处理，后端也不好排查。

---

## 3.3 你先自己写

打开：

```text
src/app/api/exception_handlers.py
```

你自己完成：

```text
1. import AppError
2. import RequestValidationError
3. 注册 app.exception_handler(AppError)
4. 注册 app.exception_handler(RequestValidationError)
5. 保留 app.exception_handler(Exception)
6. AppError 返回 exc.status_code
7. AppError 返回 error.code / error.message / error.detail
8. ValidationError 返回 422
9. Exception 返回 INTERNAL_SERVER_ERROR
10. 所有异常都写日志
```

骨架：

```python
from fastapi import FastAPI, Request
from fastapi.exceptions import RequestValidationError
from fastapi.responses import JSONResponse

from src.app.exceptions import AppError
from src.app.logger import get_logger

logger = get_logger()


def register_exception_handlers(app: FastAPI) -> None:
    @app.exception_handler(AppError)
    async def app_error_handler(request: Request, exc: AppError) -> JSONResponse:
        # 你自己写
        pass

    @app.exception_handler(RequestValidationError)
    async def validation_error_handler(
        request: Request,
        exc: RequestValidationError,
    ) -> JSONResponse:
        # 你自己写
        pass

    @app.exception_handler(Exception)
    async def global_exception_handler(request: Request, exc: Exception) -> JSONResponse:
        # 你自己写
        pass
```

---

## 3.4 检查点

先启动：

```bash
python -m src.app.main
```

然后故意传错参数：

```bash
curl -X POST http://127.0.0.1:8000/chat \
  -H "Content-Type: application/json" \
  -d '{}'
```

应该返回：

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "请求参数错误",
    "detail": [...]
  }
}
```

---

## 3.5 参考答案

```python
from fastapi import FastAPI, Request
from fastapi.exceptions import RequestValidationError
from fastapi.responses import JSONResponse

from src.app.exceptions import AppError
from src.app.logger import get_logger

logger = get_logger()


def register_exception_handlers(app: FastAPI) -> None:
    @app.exception_handler(AppError)
    async def app_error_handler(request: Request, exc: AppError) -> JSONResponse:
        logger.warning(
            "应用异常: path=%s code=%s message=%s detail=%s",
            request.url.path,
            exc.code,
            exc.message,
            exc.detail,
        )

        error = {
            "code": exc.code,
            "message": exc.message,
        }

        if exc.detail:
            error["detail"] = exc.detail

        return JSONResponse(
            status_code=exc.status_code,
            content={"error": error},
        )

    @app.exception_handler(RequestValidationError)
    async def validation_error_handler(
        request: Request,
        exc: RequestValidationError,
    ) -> JSONResponse:
        logger.warning(
            "请求参数错误: path=%s errors=%s",
            request.url.path,
            exc.errors(),
        )

        return JSONResponse(
            status_code=422,
            content={
                "error": {
                    "code": "VALIDATION_ERROR",
                    "message": "请求参数错误",
                    "detail": exc.errors(),
                }
            },
        )

    @app.exception_handler(Exception)
    async def global_exception_handler(request: Request, exc: Exception) -> JSONResponse:
        logger.exception(
            "未处理异常: path=%s error=%s",
            request.url.path,
            exc,
        )

        return JSONResponse(
            status_code=500,
            content={
                "error": {
                    "code": "INTERNAL_SERVER_ERROR",
                    "message": "服务器内部错误",
                }
            },
        )
```

---

# Day 4：修复 `/models` 错误处理

## 4.1 目标

让 `/models` 在 Ollama 异常时，不再暴露原始异常，而是返回统一错误结构。

---

## 4.2 为什么要做这个？

你最新提交日志中已经出现过：

```text
502 Server Error: Bad Gateway for url: http://localhost:11434/api/tags
```

这说明 `/models` 是一个真实风险点。

接口调用外部服务时，必须考虑：

```text
Ollama 没启动
Ollama 正在加载模型
Ollama 返回 502
网络连接失败
返回内容不是 JSON
```

---

## 4.3 你先自己写

打开：

```text
src/app/health.py
```

找到：

```python
def get_available_models() -> list[str]:
```

你自己完成：

```text
1. import LLMProviderError
2. 对 requests.get 做 try except
3. 捕获 Timeout
4. 捕获 HTTPError
5. 捕获 RequestException
6. 对 response.json() 也做 try except
7. 出错时抛 LLMProviderError
8. 成功时返回模型名称列表
```

骨架：

```python
def get_available_models() -> list[str]:
    base_url = get_ollama_base_url()
    timeout = get_ollama_timeout()

    try:
        # 你自己写 requests.get
        # 你自己写 raise_for_status
        pass
    except requests.exceptions.Timeout as exc:
        # 你自己抛 LLMProviderError
        pass
    except requests.exceptions.HTTPError as exc:
        # 你自己抛 LLMProviderError
        pass
    except requests.exceptions.RequestException as exc:
        # 你自己抛 LLMProviderError
        pass

    try:
        # 你自己解析 json
        pass
    except ValueError as exc:
        # 你自己抛 LLMProviderError
        pass

    # 你自己从 data["models"] 提取 name
```

---

## 4.4 检查点

关闭 Ollama：

```bash
pkill ollama
```

然后请求：

```bash
curl http://127.0.0.1:8000/models
```

应该返回：

```json
{
  "error": {
    "code": "LLM_PROVIDER_ERROR",
    "message": "...",
    "detail": "..."
  }
}
```

启动 Ollama 后再试：

```bash
ollama serve
```

另开一个终端：

```bash
curl http://127.0.0.1:8000/models
```

应该返回正常模型列表。

---

## 4.5 参考答案

```python
def get_available_models() -> list[str]:
    base_url = get_ollama_base_url()
    timeout = get_ollama_timeout()

    try:
        response = requests.get(f"{base_url}/api/tags", timeout=timeout)
        response.raise_for_status()
    except requests.exceptions.Timeout as exc:
        raise LLMProviderError(
            message="获取模型列表超时",
            detail=str(exc),
        ) from exc
    except requests.exceptions.HTTPError as exc:
        error_response = exc.response
        status_code = error_response.status_code if error_response is not None else "unknown"
        response_text = error_response.text if error_response is not None else str(exc)

        raise LLMProviderError(
            message="获取模型列表失败",
            detail=f"状态码: {status_code}，响应内容: {response_text}",
        ) from exc
    except requests.exceptions.RequestException as exc:
        raise LLMProviderError(
            message="Ollama 服务不可用，无法获取模型列表",
            detail=str(exc),
        ) from exc

    try:
        data = response.json()
    except ValueError as exc:
        raise LLMProviderError(
            message="Ollama 模型列表返回格式异常",
            detail=response.text,
        ) from exc

    models = data.get("models", [])
    return [item.get("name", "") for item in models if item.get("name")]
```

---

# Day 5：给 Provider 增加流式抽象

## 5.1 目标

你现在的 Provider 只能完整返回：

```python
provider.chat(...)
```

第二周要支持：

```python
provider.stream_chat(...)
```

---

## 5.2 为什么要先改抽象？

因为你后面可能不只用 Ollama，还可能接：

```text
OpenAI
vLLM
Qwen API
Claude API
本地 llama.cpp
```

如果你只在 `OllamaProvider` 里随便写一个流式方法，后面 Provider 会越来越不统一。

所以要先在抽象层定义能力：

```text
所有 LLMProvider 都应该支持 chat
所有 LLMProvider 都可以支持 stream_chat
```

---

## 5.3 你先自己写

打开：

```text
src/app/services/llm/base.py
```

你自己完成：

```text
1. import Iterator
2. 新增 dataclass：LLMStreamChunk
3. LLMStreamChunk 字段：
   - delta: str
   - done: bool = False
4. 在 LLMProvider 中新增抽象方法 stream_chat
5. stream_chat 返回 Iterator[LLMStreamChunk]
```

骨架：

```python
from collections.abc import Iterator

@dataclass
class LLMStreamChunk:
    # 你自己补
    pass


class LLMProvider(ABC):
    name: str

    @abstractmethod
    def chat(...):
        pass

    @abstractmethod
    def stream_chat(...):
        pass
```

---

## 5.4 检查点

执行：

```bash
python -m py_compile src/app/services/llm/base.py
```

如果没有输出，说明语法没问题。

但注意：你新增了抽象方法后，`OllamaProvider` 暂时会因为没实现 `stream_chat` 而不能实例化。

这很正常。

下一步修。

---

## 5.5 参考答案

```python
from abc import ABC, abstractmethod
from collections.abc import Iterator
from dataclasses import dataclass


@dataclass
class LLMUsage:
    prompt_tokens: int = 0
    completion_tokens: int = 0
    total_tokens: int = 0


@dataclass
class LLMResponse:
    content: str
    provider: str
    model: str
    usage: LLMUsage


@dataclass
class LLMStreamChunk:
    delta: str
    done: bool = False


class LLMProvider(ABC):
    name: str

    @abstractmethod
    def chat(
        self,
        messages: list[dict[str, str]],
        model: str | None = None,
    ) -> LLMResponse:
        raise NotImplementedError

    @abstractmethod
    def stream_chat(
        self,
        messages: list[dict[str, str]],
        model: str | None = None,
    ) -> Iterator[LLMStreamChunk]:
        raise NotImplementedError
```

---

# Day 6：自己实现 Ollama 流式输出

## 6.1 目标

在 `OllamaProvider` 里新增：

```python
stream_chat()
```

它要一段段返回模型输出。

---

## 6.2 你要先理解 Ollama 流式返回

非流式：

```json
{
  "message": {
    "role": "assistant",
    "content": "完整答案"
  },
  "done": true
}
```

流式：

```text
{"message":{"role":"assistant","content":"你"},"done":false}
{"message":{"role":"assistant","content":"好"},"done":false}
{"message":{"role":"assistant","content":"，"},"done":false}
{"done":true}
```

也就是说：

```text
一次 HTTP 请求
多行 JSON
一行就是一个片段
```

---

## 6.3 你先自己写

打开：

```text
src/app/services/llm/ollama_provider.py
```

你自己完成：

```text
1. import json
2. import Iterator
3. import LLMStreamChunk
4. 新增 stream_chat 方法
5. payload 里 stream 设置为 True
6. requests.post 也设置 stream=True
7. 用 response.iter_lines(decode_unicode=True) 遍历每一行
8. 每一行 json.loads
9. 如果 data.get("done") 为 True，yield done=True
10. 否则取 data["message"]["content"]
11. yield LLMStreamChunk(delta=xxx)
12. 捕获 Timeout / HTTPError / RequestException
13. 出错时抛 LLMProviderError
```

骨架：

```python
def stream_chat(
    self,
    messages: list[dict[str, str]],
    model: str | None = None,
) -> Iterator[LLMStreamChunk]:
    base_url = get_ollama_base_url()
    selected_model = model or get_selected_model()
    timeout = get_ollama_timeout()
    keep_alive = get_ollama_keep_alive()

    url = f"{base_url}/api/chat"

    payload = {
        "model": selected_model,
        "messages": messages,
        "stream": True,
        "keep_alive": keep_alive,
    }

    try:
        with requests.post(
            url,
            json=payload,
            timeout=timeout,
            stream=True,
        ) as response:
            # 你自己写
            pass

    except requests.exceptions.Timeout as exc:
        # 你自己写
        pass
    except requests.exceptions.HTTPError as exc:
        # 你自己写
        pass
    except requests.exceptions.RequestException as exc:
        # 你自己写
        pass
```

---

## 6.4 检查点

临时执行：

```bash
python
```

进入 Python 后输入：

```python
from src.app.services.llm.ollama_provider import OllamaProvider

provider = OllamaProvider()

for chunk in provider.stream_chat(
    [{"role": "user", "content": "用一句话解释什么是 AI Agent"}]
):
    if chunk.done:
        break
    print(chunk.delta, end="")
```

如果终端逐步输出内容，说明 Provider 层流式能力成功。

---

## 6.5 参考答案

```python
def stream_chat(
    self,
    messages: list[dict[str, str]],
    model: str | None = None,
) -> Iterator[LLMStreamChunk]:
    base_url = get_ollama_base_url()
    selected_model = model or get_selected_model()
    timeout = get_ollama_timeout()
    keep_alive = get_ollama_keep_alive()

    url = f"{base_url}/api/chat"

    payload = {
        "model": selected_model,
        "messages": messages,
        "stream": True,
        "keep_alive": keep_alive,
    }

    try:
        with requests.post(
            url,
            json=payload,
            timeout=timeout,
            stream=True,
        ) as response:
            response.raise_for_status()

            for line in response.iter_lines(decode_unicode=True):
                if not line:
                    continue

                try:
                    data = json.loads(line)
                except json.JSONDecodeError as exc:
                    raise LLMProviderError(
                        message="Ollama 流式返回不是合法 JSON",
                        detail=line,
                    ) from exc

                if data.get("done"):
                    yield LLMStreamChunk(delta="", done=True)
                    break

                delta = data.get("message", {}).get("content", "")

                if delta:
                    yield LLMStreamChunk(delta=delta, done=False)

    except requests.exceptions.Timeout as exc:
        raise LLMProviderError(
            message="Ollama 流式请求超时",
            detail=str(exc),
        ) from exc
    except requests.exceptions.HTTPError as exc:
        error_response = exc.response
        status_code = error_response.status_code if error_response is not None else "unknown"
        response_text = error_response.text if error_response is not None else str(exc)

        raise LLMProviderError(
            message="Ollama 流式 HTTP 请求失败",
            detail=f"状态码: {status_code}，响应内容: {response_text}",
        ) from exc
    except requests.exceptions.RequestException as exc:
        raise LLMProviderError(
            message="Ollama 流式请求失败",
            detail=str(exc),
        ) from exc
```

---

# Day 7：新增 Chat Stream Service

## 7.1 目标

Provider 层现在能流式输出了。

但接口不能直接暴露 Provider。

你还需要在 `chat_service.py` 里新增：

```python
handle_chat_stream()
```

它负责：

```text
1. 接收用户 message
2. 构造 prompt
3. 调用 provider.stream_chat
4. 把 chunk 转成 SSE 格式
5. 拼接完整答案
6. 保存聊天历史
7. 记录日志
```

---

## 7.2 为什么不直接在 routes.py 里写？

因为 `routes.py` 是接口层，应该薄一点。

职责应该是：

```text
接收请求
调用 service
返回响应
```

业务逻辑放在 service 里。

这就是后端常见分层：

```text
API Layer
  ↓
Service Layer
  ↓
Provider / Repository Layer
```

---

## 7.3 你先自己写

打开：

```text
src/app/services/chat_service.py
```

新增函数：

```python
def handle_chat_stream(message: str, model: str | None = None) -> Iterator[str]:
```

你自己完成：

```text
1. import json
2. import Iterator
3. selected_model = model or get_ollama_model()
4. logger.info 记录流式请求开始
5. prompt = build_chat_prompt(message)
6. messages = [{"role": "user", "content": prompt}]
7. provider = OllamaProvider()
8. start = perf_counter()
9. full_answer_parts = []
10. 遍历 provider.stream_chat
11. 每个 delta append 到 full_answer_parts
12. yield SSE 格式：data: {...}\n\n
13. 结束后 append_history
14. yield done payload
15. yield data: [DONE]\n\n
16. except 中 yield error payload
```

骨架：

```python
def handle_chat_stream(
    message: str,
    model: str | None = None,
) -> Iterator[str]:
    selected_model = model or get_ollama_model()
    logger.info("收到流式聊天请求，provider=ollama, model=%s", selected_model)

    prompt = build_chat_prompt(message)

    messages = [
        {
            "role": "user",
            "content": prompt,
        }
    ]

    provider = OllamaProvider()
    start = perf_counter()
    full_answer_parts: list[str] = []

    try:
        # 你自己写 for chunk in provider.stream_chat(...)
        pass
    except Exception as exc:
        # 你自己写流式错误
        pass
```

---

## 7.4 SSE 格式提示

每次 yield 的字符串必须像这样：

```python
yield f"data: {json_text}\n\n"
```

注意是两个换行：

```text
\n\n
```

不是一个。

---

## 7.5 检查点

暂时不用接路由，可以先在 Python 里测：

```python
from src.app.services.chat_service import handle_chat_stream

for item in handle_chat_stream("用一句话解释 RAG"):
    print(item)
```

你应该看到：

```text
data: {"delta": "...", "done": false}
```

---

## 7.6 参考答案

```python
def handle_chat_stream(
    message: str,
    model: str | None = None,
) -> Iterator[str]:
    selected_model = model or get_ollama_model()
    logger.info("收到流式聊天请求，provider=ollama, model=%s", selected_model)

    prompt = build_chat_prompt(message)

    messages = [
        {
            "role": "user",
            "content": prompt,
        }
    ]

    provider = OllamaProvider()
    start = perf_counter()

    full_answer_parts: list[str] = []

    try:
        for chunk in provider.stream_chat(messages=messages, model=selected_model):
            if chunk.done:
                break

            full_answer_parts.append(chunk.delta)

            payload = {
                "delta": chunk.delta,
                "done": False,
            }

            yield f"data: {json.dumps(payload, ensure_ascii=False)}\n\n"

        elapsed = perf_counter() - start
        full_answer = "".join(full_answer_parts)

        append_history(
            {
                "timestamp": datetime.now().isoformat(),
                "model": selected_model,
                "user_input": message,
                "prompt": prompt,
                "answer": full_answer,
                "elapsed_seconds": round(elapsed, 2),
            }
        )

        done_payload = {
            "delta": "",
            "done": True,
            "model": selected_model,
            "latency_ms": int(elapsed * 1000),
        }

        yield f"data: {json.dumps(done_payload, ensure_ascii=False)}\n\n"
        yield "data: [DONE]\n\n"

        logger.info("流式聊天请求处理完成，耗时 %s ms", int(elapsed * 1000))

    except Exception as exc:
        logger.exception("流式聊天请求失败: %s", exc)

        error_payload = {
            "error": {
                "code": "STREAM_CHAT_ERROR",
                "message": "流式聊天失败",
                "detail": str(exc),
            }
        }

        yield f"data: {json.dumps(error_payload, ensure_ascii=False)}\n\n"
        yield "data: [DONE]\n\n"
```

---

# Day 8：新增 `/chat/stream` 路由

## 8.1 目标

让浏览器或 curl 能请求：

```text
POST /chat/stream
```

并看到流式输出。

---

## 8.2 你先自己写

打开：

```text
src/app/api/routes.py
```

你自己完成：

```text
1. 从 fastapi.responses import StreamingResponse
2. 从 chat_service import handle_chat_stream
3. 新增 @router.post("/chat/stream")
4. 接收 ChatRequest
5. 返回 StreamingResponse
6. media_type 设置为 text/event-stream
7. headers 加：
   - Cache-Control: no-cache
   - Connection: keep-alive
   - X-Accel-Buffering: no
```

骨架：

```python
@router.post("/chat/stream")
def chat_stream(request: ChatRequest) -> StreamingResponse:
    # 你自己写
    pass
```

---

## 8.3 检查点

启动：

```bash
python -m src.app.main
```

请求：

```bash
curl -N -X POST http://127.0.0.1:8000/chat/stream \
  -H "Content-Type: application/json" \
  -d '{"message":"用三句话解释什么是 Agent"}'
```

你应该看到内容一点点输出。

---

## 8.4 参考答案

```python
@router.post("/chat/stream")
def chat_stream(request: ChatRequest) -> StreamingResponse:
    return StreamingResponse(
        handle_chat_stream(
            message=request.message,
            model=request.model,
        ),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "Connection": "keep-alive",
            "X-Accel-Buffering": "no",
        },
    )
```

---

# Day 9：修复 `/chat` 忽略 model 的问题

## 9.1 目标

你当前 `ChatRequest` 里已经有：

```python
model: str | None
```

但 `/chat` 之前调用时可能只传了：

```python
handle_chat(request.message)
```

这会导致用户传 `model` 没有效果。

---

## 9.2 你先自己改

找到 `/chat`：

```python
@router.post("/chat", response_model=ChatResponse)
def chat(request: ChatRequest) -> ChatResponse:
```

把调用改成：

```python
handle_chat(request.message, model=request.model)
```

---

## 9.3 检查点

请求：

```bash
curl -X POST http://127.0.0.1:8000/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"你好","model":"你的模型名"}'
```

看返回里的：

```json
"model": "你的模型名"
```

是否正确。

---

# Day 10：日志验证

## 10.1 目标

确认日志能记录：

```text
普通聊天请求
流式聊天请求
模型名称
耗时
异常
```

---

## 10.2 你自己操作

开一个终端：

```bash
tail -f logs/app.log
```

另一个终端请求：

```bash
curl -X POST http://127.0.0.1:8000/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"什么是 RAG？"}'
```

再请求：

```bash
curl -N -X POST http://127.0.0.1:8000/chat/stream \
  -H "Content-Type: application/json" \
  -d '{"message":"什么是 Agent？"}'
```

你应该在日志里看到类似：

```text
收到聊天请求，provider=ollama, model=xxx
聊天请求处理完成，耗时 xxx ms
收到流式聊天请求，provider=ollama, model=xxx
流式聊天请求处理完成，耗时 xxx ms
```

---

# Day 11：写 README，而不是只写代码

## 11.1 目标

把第二周成果写进 README。

因为以后面试时，你不能只说“我写了代码”，你要能讲：

```text
我为什么这么设计
我解决了什么工程问题
我怎么验证
```

---

## 11.2 你先自己写

在 README 里新增：

```md
## Week 2：Streaming + 配置系统 + 日志系统 + 统一异常处理
```

至少写这几部分：

```text
1. 本周目标
2. 新增接口
3. 新增文件
4. 核心概念
5. 测试命令
6. 常见错误
7. 本周总结
```

---

## 11.3 README 骨架

你可以按这个写，但内容尽量自己组织：

````md
## Week 2：Streaming + 配置系统 + 日志系统 + 统一异常处理

### 本周目标

把第一周的最小模型调用服务升级成更接近真实 AI 产品的后端服务。

### 新增能力

- 统一 Settings 配置对象
- 统一 AppError 异常结构
- 修复 `/models` 的 Ollama 异常处理
- 新增 `/chat/stream` SSE 流式输出
- 增强日志记录

### 普通聊天接口

```bash
curl -X POST http://127.0.0.1:8000/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"什么是 RAG？"}'
````

### 流式聊天接口

```bash
curl -N -X POST http://127.0.0.1:8000/chat/stream \
  -H "Content-Type: application/json" \
  -d '{"message":"什么是 Agent？"}'
```

### 错误返回结构

```json
{
  "error": {
    "code": "LLM_PROVIDER_ERROR",
    "message": "模型服务调用失败",
    "detail": "..."
  }
}
```

### 本周理解

这周重点不是功能数量，而是把模型服务变得更稳定、更可观测、更接近真实 AI 产品。

````

---

# Day 12：最终验收

你要按顺序跑完这些命令。

## 12.1 启动服务

```bash
python -m src.app.main
````

---

## 12.2 测 `/health`

```bash
curl http://127.0.0.1:8000/health
```

---

## 12.3 测 `/models`

```bash
curl http://127.0.0.1:8000/models
```

---

## 12.4 测 `/chat`

```bash
curl -X POST http://127.0.0.1:8000/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"用一句话解释什么是 RAG"}'
```

---

## 12.5 测 `/chat/stream`

```bash
curl -N -X POST http://127.0.0.1:8000/chat/stream \
  -H "Content-Type: application/json" \
  -d '{"message":"用三句话解释什么是 AI Agent"}'
```

---

## 12.6 测参数错误

```bash
curl -X POST http://127.0.0.1:8000/chat \
  -H "Content-Type: application/json" \
  -d '{}'
```

---

## 12.7 测 Ollama 异常

关闭 Ollama 后：

```bash
curl http://127.0.0.1:8000/models
```

应该看到统一错误结构。

---

# 第二周你最终应该提交什么

## 文件层面

```text
src/app/config.py
src/app/exceptions.py
src/app/api/exception_handlers.py
src/app/health.py
src/app/services/llm/base.py
src/app/services/llm/ollama_provider.py
src/app/services/chat_service.py
src/app/api/routes.py
.env.example
README.md
pyproject.toml
uv.lock
```

---

## 能力层面

你应该能独立讲清楚：

```text
1. 为什么要用 Settings
2. 为什么要保留旧 get_xxx 函数
3. 为什么要定义 AppError
4. 为什么 Ollama 错误要转成 LLMProviderError
5. 为什么流式接口用 StreamingResponse
6. 为什么 SSE 要 data: xxx\n\n
7. 为什么 curl 测流式要加 -N
8. 为什么流式接口中途报错不能完全依赖全局异常处理
9. 为什么 /models 要单独处理 requests 异常
10. 为什么 Provider 层要先定义 stream_chat 抽象
```

---

# 推荐提交信息

```bash
git add .

git commit -m "Add streaming chat and unified error handling"
```

---

# 本周最重要的学习原则

你这周不要追求“复制出正确代码”。

你要追求：

```text
看到目标
自己写第一版
运行报错
读报错
修正
再运行
最后对比参考答案
```

真正的后端能力不是记住代码，而是能处理这类问题：

```text
外部服务挂了怎么办？
接口怎么稳定返回？
日志怎么查？
流式输出怎么不断开？
配置怎么不写死？
后续换模型服务怎么少改代码？
```

这才是第二周的核心。
